知乎账号矩阵自动化发布系统

产品文档 v1.7

文档状态：链路已跑通，定时发布待补验

文档日期：2026-03-27

负责人：二牛

配套文档：
1. `知乎账号矩阵化改造实施指南.md`
2. `飞书通知功能说明.md`


一、文档定位

这份文档只保留当前已经落地、并且准备进入生产环境的统一口径，不再展开历史方案推演、长篇 Prompt 草案和未落地设想。

这里的判断标准很简单：

1. 代码已经接入。
2. 本地真实链路已经跑通。
3. 关键结果能在任务详情、草稿、审核记录和最终发布链接里追溯。


二、本次联调结论

截至 2026-03-27，系统已经完成一轮可追溯的真实联调，覆盖五个部分：

1. 选题
2. 写作
3. 审核
4. 矩阵化自动发布
5. 飞书消息

当前可以确认的事实：

1. 两个账号都已经完成真实发布，说明矩阵账号不是只停留在数据模型层。
2. 已有任务可以从 `topic card -> draft -> review -> publish job -> 最终知乎链接` 一路追溯，证据链完整。
3. 发布中断后的恢复链路已经验证，不是只能在理想路径里成功。
4. 飞书测试接口已经打通，人工已确认可以收件；本轮真实发布期间没有观测到飞书失败日志。
5. `npm run typecheck` 和 `npm run build` 已通过。
6. “按时自动发布”专项验收本轮没有通过。`job 21`、`job 20`、`job 18` 在未来时间点到达时，仍停留在 `topic_review`、`writer`、`humanizing`、`review_hard_gate` 等准备阶段，没有收口到 `review_passed` 或 `published`。

结论写得更直接一点：

`在“单机部署 + 多账号串行执行 + 人工登录恢复 + 飞书最小通知”的当前范围内，系统已经具备真实运行能力；但“按时自动发布”还不能按生产级能力对外承诺。`

这句话的边界也要说清楚：

1. 现在已经不是“只够开发自测”，五段链路都已经有真实样本。
2. 现在还不能把“严格准点自动发布”写成已验收能力。
3. 如果立刻上线，应按“可运行 + 可人工盯盘”的口径使用，而不是按“完全无人值守、严格准点”承诺。


三、真实验证样本

这次联调不是抽象判断，而是有真实样本支撑。

样本如下：

1. `job 8`
   账号：`Default Zhihu Account`
   知乎账号：`二牛是个老实人`
   状态：`published`
   题目：`在投资中，“完全分类”思想真的能取代“预测”吗？`
   链接：`https://www.zhihu.com/question/2019917855274091317/answer/2020539096838186856`

2. `job 11`
   账号：`Default Zhihu Account`
   知乎账号：`二牛是个老实人`
   状态：`published`
   题目：`初入币圈的小白有什么建议？`
   链接：`https://www.zhihu.com/question/284889810/answer/2020892925748417886`

3. `job 13`
   账号：`焦花花`
   知乎账号：`焦花花`
   状态：`published`
   题目：`你人生中太晚明白的道理是什么？`
   链接：`https://www.zhihu.com/question/470076571`

4. `job 19`
   账号：`焦花花`
   知乎账号：`焦花花`
   状态：`published`
   题目：`加密货币市场的未来潜力还有多大？`
   链接：`https://www.zhihu.com/question/4606985456/answer/2020853884906664286`
   说明：这个样本验证了发布中断后的恢复能力，最终仍然收口到 `published`。

补充观察：按时自动发布专项未通过，现场记录如下：

1. `job 21` 在 `2026-03-27 16:40:07 +08:00` 到点时，状态仍是 `topic_review`。
2. `job 20` 改到 `2026-03-27 16:50:30 +08:00` 后，到点前后仍停留在 `writer`、`humanizing`、`review_hard_gate` 等准备阶段。
3. `job 18` 改到 `2026-03-27 17:10:30 +08:00` 后，到 `2026-03-27 17:13:21 +08:00` 仍停在 `topic_review`。


四、五段链路当前口径

### 4.1 选题

当前选题已经不是“人工先定题，系统只负责发”。

当前口径是：

1. 系统会先做题目采集和筛选，再生成 `topic card`。
2. `topic card` 会固化题目摘要、优先级、人设模式、风险提醒和 `topic fingerprint`。
3. 真实样本里已经能看到 `topicCardId` 和 `topicOutputJson`，说明选题结果已经进入任务主链路，而不是停留在临时草稿层。

### 4.2 写作

当前写作链路已经不是只保留一段最终正文。

当前口径是：

1. 系统会保留 `raw draft`、`humanized draft` 和 `approved content`。
2. job 会固化自己的 Prompt 快照，避免后续 Prompt 变更污染已创建任务。
3. 真实样本里已经能看到完整正文和对应快照，说明写作阶段可追溯、可复盘。

### 4.3 审核

当前审核已经进入正式主链路，不是“写完直接发”。

当前口径是：

1. 审核结果会写入 `review` 记录。
2. 只有 `review_status = pass` 的内容才进入发布阶段。
3. 审核会同时保留硬性风控、编辑质量和发布前判断的结果。

### 4.4 矩阵化自动发布

当前发布链路已经具备矩阵口径。

当前口径是：

1. 每个账号使用自己的独立 `Profile`，长期复用登录资产。
2. 不同账号已经有真实成功样本，说明矩阵不是单账号伪装。
3. 发布成功不是只看 URL，而是结合页面语义、正文命中和结果核验来收口。
4. 成功收口统一落到 `published`，并写回最终链接。
5. 发布中断后的恢复已经验证通过，`job 19` 是这次最关键的恢复样本。
6. 当前“有排期字段”不等于“准点发布已经验收通过”。本轮未来时间样本显示，任务可能在到点时仍未进入可发布态。

### 4.5 飞书消息

当前飞书只保留最小通知范围，但已经正式接入主链路。

当前口径是：

1. 问题通知在 `manual_login_required` 和 `failed_terminal` 这些关键状态发送。
2. 发布成功通知在 job 最终进入 `published` 后发送。
3. 所有账号共用一个机器人，但消息里会明确带上矩阵账号、知乎账号、任务 ID 和阶段信息。
4. 配置使用 `FEISHU_BOT_WEBHOOK_URL` 和 `FEISHU_BOT_SECRET`。
5. 飞书失败不会阻塞主业务，只记录错误并返回 `failed`。


五、当前适合灰度运行的范围

当前已经实测通过、适合先灰度运行的，不是“全部运营能力”，而是下面这套范围：

1. 多账号模型
2. 账号独立 Profile
3. 账号级 Writer Prompt 快照
4. 账号级排期模型
5. 选题、写作、审核、发布的任务证据链
6. 矩阵控制台查看与人工恢复
7. 飞书最小通知

如果换成更实操的说法，当前适合进入灰度运行环境的使用方式是：

1. 单个后端实例
2. 单个 worker 实例
3. 多账号串行执行
4. 需要人工登录恢复时由人介入
5. 发布成功和关键异常通过飞书收口
6. 对时间敏感任务保留人工观测，暂不承诺严格准点自动发布


六、上线前提和残余风险

可以进生产，不等于没有注意事项。

上线前提如下：

1. 每个生产账号都已经完成至少一次人工登录确认。
2. 每个账号的 `Profile` 已经真实落盘并稳定复用。
3. 生产环境已经配置好飞书 `Webhook` 和 `Secret`。
4. worker 维持单实例运行，不和手工调试动作抢同一个账号 Profile。

当前还保留三条需要明确记录的尾项：

1. 旧的中断 `publish_attempts` 在恢复成功后，可能残留早期记录为 `running`。
   这不会阻塞主链路，也不影响最终 `published`，但会影响运维排查时的数据整洁度。
2. 当后台 worker 正在运行时，不建议再手工调用 `/worker/tick` 去抢同一个账号的发布流程。
   这不是主链路缺失，而是运行方式上要坚持单 worker 口径。
3. 准备队列当前一次只推进 1 个任务，而且按计划时间顺序取最早任务。前面如果有更早但尚未准备完成的任务，后面的未来任务可能在到点前仍停留在准备阶段。本轮 `job 20` 和 `job 18` 已真实观测到这种阻塞。

这些尾项不等于系统完全不能继续运行，但如果业务要求严格定时发布，就不能直接按当前实现进入正式生产。


七、下一阶段再做什么

当前这轮工作已经完成，下一阶段不再是“先把矩阵底座做出来”，而是继续往上叠运营能力和运维能力。

下一阶段重点如下：

1. 先修正准备队列的阻塞问题，再补做“未来时间点自动发布”专项验收。
2. 清理恢复后遗留的旧 `publish_attempts` 运行态。
3. 做更完整的通知观测，而不是只保留最小飞书消息。
4. 做更细的并发调度、冷却和账号健康度控制。
5. 做发布后数据回收，例如 24h、48h、7d 的结果追踪。


八、最终结论

截至 2026-03-27，这个项目已经不是“只会演示的矩阵改造”，而是“真实链路已经跑通的矩阵发布系统”；但“按时自动发布”还没有达到可直接对外承诺的生产口径。

最终统一结论如下：

1. 五段链路已经真实跑通：选题、写作、审核、矩阵化自动发布、飞书消息。
2. 多账号样本已经验证成功，不是单账号伪矩阵。
3. 发布中断恢复已经验证成功，说明链路具备生产运行所需的基本韧性。
4. “按时自动发布”专项验收未通过，根因已经明确暴露为准备链路可能被更早的未完成任务阻塞。
5. 当前适合继续灰度运行或受控生产运行，但不建议在修复前承诺完全无人值守、严格准点的自动发布。

一句话收尾：

`现在的系统已经能真实跑起来，但离“严格按时、可放心无人值守”的生产状态还差一步，下一步重点是先把排期准备阻塞问题补齐。`
