知乎账号矩阵自动化发布系统

产品文档 v1.3

文档状态：MVP 跑通后扩展定义稿

文档日期：2026-03-24

负责人：二牛


一、这版先做什么

单账号 MVP 已经跑通。
这份文档在保留单账号实现口径的基础上，开始把后续矩阵化扩展先定义清楚。

当前不急着把系统粗暴扩成“多开几个账号”，而是先把三件事讲明白：

1. 什么叫矩阵

2. 矩阵里的定时发布怎么定义

3. 系统如何在每个阶段和操作者实时同步

本期目标只有一句话：

在单账号链路已经跑通的前提下，冻结矩阵化、定时发布和操作者通知的产品口径，让后续扩展是按策略复制，而不是只按账号数量复制。


二、核心要求确认

本系统必须满足以下要求。

1. 所有部分必须模块化，任何一个环节都可以单独替换。

2. 尽可能通过 LLM 解决理解、判断、归因问题，不依赖正则或脆弱脚本规则。

3. 发布环节只使用一个浏览器 MCP 工具：Playwright，并固定使用可见模式；浏览器 channel 保持可配置，不再把 Chrome 写死在产品口径里，当前运行环境允许使用 Edge。

4. 发布时一次性写入全文，不分段写入。

5. 登录信息优先复用本地浏览器 profile 和已保存 session；只有出现 `login_required`、`session_expired`、`challenge` 时，才进入人工登录恢复。

6. 前端必须能看到任务状态、发布结果、失败原因、截图、tool trace 和 skill run。

7. LLM provider 与 model 必须配置化，不写死在代码里；项目级允许接兼容 OpenAI 的 API 网关，单账号知乎 Agent 使用独立配置运行。

8. skill 依赖尽量少，MVP 只保留 `humanizer-zh` 和浏览器操作 skill 适配层。

9. 发布成功判定不能只看 URL，必须结合页面语义和正文内容命中情况一起判断。

10. 矩阵化不是简单多开账号，而是“多账号 + 多人设 + 多选题视角 + 同一推广目标”的协同系统。

11. 任务必须支持 `scheduled_at` 定时发布，并为后续多账号错峰、冷却和排期预留能力。

12. 系统必须在关键阶段向操作者发送飞书通知；无论成功、失败还是进入人工接管，都要有结构化同步消息。

13. 通知发送失败不能静默吞掉，要有发送记录、失败原因和重试能力。


三、MVP 范围

这版执行范围仍以单账号为主。

虽然当前不直接启动多账号执行，但本文开始明确第二阶段的矩阵定义、定时发布和操作者交互口径，作为后续扩展的冻结标准。

不做的内容如下。

1. 不做多账号并发执行和复杂调度落地。

2. 不做完整的矩阵式内容差异化自动生成。

3. 不做自动绕过验证码、短信验证、人机验证。

4. 不做知乎站内评论监测和自动互动。

5. 不做复杂 BI 大屏。

6. 不做知乎以外平台。

7. 不做无头发布。

补充说明：

这里“不做自动互动”特指不做知乎评论区、私信、点赞等站内自动交互。
系统对操作者的飞书同步属于当前阶段要明确的产品内交互，不算在该禁区内。


三点一、什么叫矩阵

矩阵不是“同一篇内容换多个账号发”，而是围绕同一推广目标，使用多个账号从不同身份、不同角度、不同发布时间去完成内容覆盖。

一个合格的知乎账号矩阵，至少同时满足以下四点。

1. 多个账号对应不同人设。每个账号都要有清晰身份边界，例如实操派、踩坑复盘派、方法论科普派，而不是同一种语气复制粘贴。

2. 多个账号在选题上有不同考量。有人负责工具直问型题目，有人负责教育型题目，有人负责案例型题目，但最终都服务同一推广目标。

3. 账号之间要避免同题、同角度、同节奏撞车。矩阵的价值是覆盖更多语义入口和信任路径，不是批量制造重复内容。

4. 矩阵必须和排期、冷却、通知一起设计。没有发布节奏管理和过程可见性，多账号只会把风险放大，不算真正矩阵。

一句话定义：

矩阵 = 多账号 × 多人设 × 多选题策略 × 统一推广目标 × 可调度的发布时间。

矩阵里的最小运营单元，不只是一个账号，而是“账号 + 人设 + 选题策略 + 发布时间窗口”。


三点二、矩阵里的定时发布

定时发布在矩阵里不是可选项，而是基础能力。

原因有三点：

1. 避免多个账号在同一时间扎堆发布，降低平台侧异常信号。

2. 让不同人设按各自节奏出现，形成更自然的曝光路径。

3. 便于把推广动作拆成预热、主打、补充、复盘等时间序列。

产品口径上，一个 `publish_job` 至少支持两种创建方式：

1. 立即发布

2. 指定 `scheduled_at` 到点发布

后续进入多账号阶段后，调度器还要同时考虑账号冷却、发布时间窗口、同题去重和失败重排。


三点三、这里说的交互是什么

这里的“交互”当前优先不是知乎站内自动评论或私信，而是系统对操作者的实时同步。

也就是说，系统每推进一个关键阶段，都要主动告诉你“谁做完了什么、结果怎样、下一步是什么”。

飞书通知的最小口径如下。

1. 每个关键阶段完成时发一条。至少覆盖 `topic_done`、`draft_done`、`review_passed | review_revise | review_block`、`publish_started`、`published`、`publish_failed`、`manual_login_required`。

2. 阶段失败时也要发一条，不能只发成功。

3. 消息里要明确看到：任务 id、账号名、人设、当前 Agent、阶段结果、下一步动作、失败原因；如果有链接或截图，也要带上。

4. 例如 Agent “二牛”完成了选题、完成了写作、完成了审核、完成了发布，或者未完成发布，都应同步到飞书。

5. 飞书通知属于“操作者同步通道”，不阻塞主流程；如果发送失败，要记录日志并重试，不能悄悄丢失。


四、MVP 的最小闭环

一条任务从头到尾按下面流程推进。

1. Topic Agent 负责采题预筛、批次排序，并产出题目卡。

2. Writer Agent 根据题目卡生成草稿，并在需要时调用 `humanizer-zh` 做去 AI 味处理。

3. Review Agent 审核草稿，执行 Hard Gate、Editorial Review、Publish Review 和重复性判断，输出通过或退回。

4. Publish Agent 负责登录检查、发布页理解、发布动作规划、发布后核验，并通过 Playwright Runtime 执行浏览器动作。

5. Orchestrator + Failure Resolution 在失败时归因，决定重试、换题、回退审核或人工登录恢复，不再单独对外暴露为主 Agent。

6. 前端把每一步的状态、截图、反馈、skill runs、tool traces 和通知记录展示出来。


五、系统架构

MVP 采用八层架构，并把阶段事件与通知服务作为 Orchestrator 的旁路能力。

1. 前端层

一个轻量后台，用来查看任务、内容、发布结果、失败反馈和通知历史。

2. API 层

负责接收前端请求、触发 Agent、读取状态、写入数据库，并提供立即任务与定时任务入口。

3. Orchestrator 层

负责状态机推进、任务编排、失败回滚和阶段事件发射。

4. Agent 层

只保留四个核心 Agent：Topic Agent、Writer Agent、Review Agent、Publish Agent。

5. Skill 层

只保留最少量的外部技能依赖，MVP 仅使用 `humanizer-zh` 和浏览器 skill 适配层。

6. Tool Runtime 层

负责把 Agent 的动作请求转成 Playwright 工具调用，并隔离具体浏览器执行细节；MCP 工具固定只保留 `playwright`。

7. 执行层

包括 Playwright 执行器、可见模式浏览器会话、LLM 调用网关和飞书通知执行器。

8. 存储层

使用 MySQL 存任务、状态、阶段事件和通知记录，使用本地文件目录存草稿、截图、快照和日志。


六、技术选型

MVP 建议如下。

1. 前端：Next.js

原因是开发快、页面与 API 可以同仓库维护，适合先做内部后台。

2. 后端 API：Fastify

负责对前端暴露接口、写入数据库、提供 Dashboard 和 Job 详情查询。

3. Worker：独立 Node 进程

负责推进状态机、调度 due jobs、执行失败恢复、重试逻辑和通知投递。

4. 数据库：MySQL 5.7

原因是当前单账号 MVP 已按 MySQL 5.7 落地，状态机、任务锁、查询统计都更适合关系型数据库。

5. LLM：兼容 OpenAI 协议的可配置 Provider

模型、Base URL、API Key 都走项目配置，不把模型名散落在业务代码中；当前知乎 Agent 可按独立配置使用 `qwen3.5-plus` 或其他兼容模型。

6. 浏览器执行：Playwright

只用可见模式，浏览器 channel 保持可配置；当前运行环境允许使用 Edge，不再把 Chrome 作为唯一口径。

7. 操作者通知：Feishu Bot / Webhook

原因是接入简单，适合先把阶段性同步打通；后续如果需要企业内部多渠道，再继续扩展。

8. 文件存储：本地目录

先把截图、草稿、页面快照、失败记录存本地，后续再切对象存储。


六点一、Playwright Tool Runtime

这部分是文档里必须单独说明的，因为发布系统不是直接让 Agent 硬连浏览器，而是通过一层 Playwright 工具运行时来做隔离。

MVP 里，Playwright Tool Runtime 的职责如下。

1. 对外暴露统一工具接口，不让业务代码直接耦合某个具体浏览器 SDK。

2. 记录每一次工具调用，包括输入参数、执行结果、耗时、是否成功。

3. 管理可见模式浏览器会话。

4. 提供最小但稳定的一组浏览器动作。

5. 为失败归因保留完整工具轨迹。

MVP 的浏览器工具只保留一个：`playwright`

`playwright` 这个单一工具对外暴露若干动作，动作建议收敛为以下几类。

1. `open`

打开页面。

2. `snapshot`

获取页面结构和可交互元素摘要，供 LLM 判断当前页面状态。

3. `click`

点击按钮、链接、输入框等元素。

4. `focus`

聚焦到编辑器或特定输入区域。

5. `paste_text`

一次性粘贴全文。这是本系统推荐的正文写入动作。

6. `type`

作为兜底动作，仅在 paste 不可用时考虑，但 MVP 优先走全文粘贴。

7. `press`

执行键盘动作，例如快捷键或确认键。

8. `wait`

等待页面稳定、等待弹窗消失、等待跳转完成。

9. `get_url`

获取当前页面 URL，用于发布结果确认。

10. `screenshot`

保存截图，供前端展示和失败归因。

MVP 明确禁止以下动作。

1. 不用 eval 注入页面脚本。

2. 不用 DOM 直接改内容。

3. 不用无头发布。

4. 不做验证码绕过。

Playwright Tool Runtime 的核心规则如下。

1. 每次明显页面变化后都要重新 snapshot。

2. Agent 不直接猜页面，必须基于 snapshot 或截图做判断。

3. 如果页面状态不明确，优先停止，不冒进点击。

4. 所有工具调用都要可追踪、可回放、可归因。


六点二、Skill Layer

这版不引入太多 skill，MVP 只保留两个。

1. `humanizer-zh`

用途：

用于写作阶段的去 AI 味处理。它不是独立 Agent，而是 Writer Agent 的一个可选后处理能力。

适用位置：

1. Writer Agent 生成初稿后

2. Review Agent 判断“内容基本可发但 AI 味偏重”时

使用原则：

1. 只做语言风格自然化，不改写事实边界

2. 不允许把产品能力写超

3. 不允许改变原有立场和结论

2. `playwright`

用途：

作为唯一的浏览器操作 skill，负责承载页面打开、点击、聚焦、全文粘贴、截图、读取 URL 等动作。

适用位置：

1. Publish Agent 在采题前和发布前执行登录态检查

2. Publish Agent 执行知乎发布

3. Failure Resolution 读取失败上下文时参考工具轨迹

使用原则：

1. 只用可见模式

2. 浏览器 channel 可配置，当前运行环境允许 Edge

3. 不通过脚本注入页面

4. 所有页面判断先看 snapshot，再做动作

一句话关系就是：

Agent 决策，少量 skill 复用，Playwright 执行浏览器动作。


六点三、Skill 与 Playwright 调用协议

为了让系统真正模块化，MVP 里需要把 skill 调用和 Playwright 工具调用都标准化。

Skill 调用建议统一成以下结构。

```json
{
  "skill_name": "humanizer-zh",
  "skill_version": "v1",
  "job_id": "job_xxx",
  "input": {},
  "context": {},
  "options": {
    "model": "gpt-xxx",
    "temperature": 0.2
  }
}
```

Skill 返回结果建议统一成以下结构。

```json
{
  "success": true,
  "skill_name": "humanizer-zh",
  "skill_version": "v1",
  "output": {},
  "artifacts": [],
  "usage": {
    "model": "gpt-xxx"
  },
  "error": null
}
```

Playwright 工具调用建议统一成以下结构。

```json
{
  "tool_name": "playwright",
  "action": "snapshot",
  "session_id": "session_xxx",
  "args": {},
  "trace_id": "trace_xxx"
}
```

Playwright 工具返回建议统一成以下结构。

```json
{
  "success": true,
  "tool_name": "playwright",
  "action": "snapshot",
  "trace_id": "trace_xxx",
  "result": {},
  "artifact_paths": [],
  "error": null
}
```

MVP 中推荐的调用链如下。

1. 前端创建任务

2. API 写入 `publish_jobs`

3. Orchestrator 读取任务并选择当前 Agent

4. Agent 调用对应 skill

5. Agent 如需页面动作，则通过 Tool Runtime 调 `playwright`

6. Tool Runtime 返回页面快照、截图、URL 或动作执行结果

7. Agent 或 skill 结合 LLM 输出结构化结论

8. Orchestrator 根据结论推进状态

这套协议的目标只有一个：

以后就算替换模型、或更换去 AI 味 skill，实现层也不用跟着重写；而浏览器侧在 MVP 中固定为 Playwright + 可见模式浏览器会话。


七、目录建议

建议项目目录如下。

```text
app/
  dashboard/
  topics/
  drafts/
  jobs/
  account/

src/
  agents/
    topic/
    writer/
    review/
    publish/
    failure/
    session/
  skills/
    humanizer-zh/
    playwright/
  orchestrator/
  scheduler/
  notifications/
  llm/
  playwright/
  tool-runtime/
  db/
  schemas/
  services/

data/
  drafts/
  screenshots/
  snapshots/
  logs/

docs/
```


八、模块划分

8.1 Topic Agent

职责：

1. 读取候选问题信息。

2. 用 LLM 判断是否值得回答。

3. 输出结构化题目卡。

8.2 Writer Agent

职责：

1. 根据题目卡生成知乎草稿。

2. 输出完整正文和写作说明。

8.3 Review Agent

职责：

1. 审核内容是否可发。

2. 标注风险点和必须修改项。

3. 输出通过版终稿。

8.4 Publish Agent

职责：

1. 在采题前和真正发布前执行登录态检查。

2. 根据页面快照判断当前是题目页、编辑态、已有回答态、登录/风控页还是异常页。

3. 规划下一步安全动作，并通过浏览器 skill 调用 `playwright` Runtime。

4. 在出现“查看我的回答”“编辑回答”等已有回答语义时，执行已有回答分支和内容一致性判断。

5. 保存截图、URL、发布核验结果和结构化发布记录。

8.5 Orchestrator / Failure Resolution

职责：

1. 按状态推进任务。

2. 保证单任务串行执行。

3. 在失败时决定 `RETRY_SAME_SESSION`、`RESTART_BROWSER`、`VERIFY_ONCE`、`REWRITE_ONCE`、`RESELECT_TOPIC`、`MANUAL_LOGIN` 或 `TERMINAL_FAIL`。

4. 统一记录运行日志、恢复锚点、阶段事件和最终归因。

8.6 Skill Registry

职责：

1. 统一注册系统依赖的少量 skill。

2. 根据 skill 名称加载配置、输入输出协议和权限。

3. 控制版本，避免同名 skill 被随意覆盖。

4. 为前端和日志提供 skill 调用可视化信息。

8.7 Playwright Executor

职责：

1. 执行标准化 `playwright` 工具调用。

2. 对可见模式浏览器动作做统一封装，浏览器 channel 保持可配置。

3. 记录每一步工具轨迹。

4. 给 Publish 相关能力提供底层执行支撑。

8.8 Scheduler

职责：

1. 扫描 `scheduled_at <= now` 的待执行任务。

2. 在单账号阶段保证到点触发；在矩阵阶段负责错峰、冷却和冲突避让。

3. 把 due job 交给 Orchestrator，并记录调度决策。

8.9 Notification Service

职责：

1. 监听阶段事件并组装操作者通知消息。

2. 向飞书发送结构化通知。

3. 记录发送结果、重试状态和失败原因。

4. 保证通知失败不影响主流程，但对前端和日志可见。


九、MVP 状态机

MVP 任务状态如下。

1. `queued`

2. `topic_discovery`

3. `topic_agent`

4. `topic_review`

5. `writer`

6. `humanizing`

7. `review_hard_gate`

8. `review_editorial`

9. `review_publish`

10. `review_passed`

11. `login_checking`

12. `publishing`

13. `publish_verify`

14. `retry_waiting`

15. `manual_login_required`

16. `published`

17. `failed_terminal`

说明：

这版只有一个账号，但状态机从第一天就保留扩展能力，后面切多账号时不用重写整套流程。

补充规则：

每次进入或离开关键阶段时，都要产出一条阶段事件记录，并驱动一次飞书通知尝试。


十、MVP 数据表

建议先建九张核心表。

1. `accounts`

保存账号基本信息。

关键字段：

`id`
`name`
`status`
`persona_name`
`persona_summary`
`publish_window_json`
`cooldown_until`
`profile_dir`
`last_login_check_at`
`last_publish_at`
`created_at`
`updated_at`

2. `topics`

保存题目卡。

关键字段：

`id`
`question_url`
`question_title`
`question_summary`
`priority`
`fit_score`
`topic_json`
`created_at`

3. `drafts`

保存写作结果。

关键字段：

`id`
`topic_id`
`content`
`word_count`
`style_notes`
`draft_json`
`created_at`

4. `reviews`

保存审核结果。

关键字段：

`id`
`draft_id`
`status`
`score`
`must_fix_items_json`
`risk_tags_json`
`approved_content`
`review_json`
`created_at`

5. `publish_jobs`

保存发布任务。

关键字段：

`id`
`account_id`
`persona_name_snapshot`
`topic_card_id`
`review_id`
`question_url`
`status`
`schedule_mode`
`scheduled_at`
`started_at`
`finished_at`
`final_url`
`final_url_type`
`prompt_version_snapshot_json`
`resume_anchor_json`
`notify_policy_json`
`created_at`

6. `publish_attempts`

保存每次发布尝试。

关键字段：

`id`
`publish_job_id`
`attempt_no`
`status`
`current_url`
`result_mode`
`failure_type`
`failure_reason`
`attempt_json`
`created_at`

7. `artifacts`

保存截图、快照、日志路径。

关键字段：

`id`
`publish_attempt_id`
`artifact_type`
`file_path`
`meta_json`
`created_at`

8. `job_stage_events`

保存每个任务在每个阶段产生的事件流。

关键字段：

`id`
`publish_job_id`
`account_id`
`agent_name`
`persona_name`
`stage_key`
`stage_status`
`summary`
`detail_json`
`created_at`

9. `operator_notifications`

保存发给操作者的通知记录。

关键字段：

`id`
`event_id`
`channel`
`receiver`
`send_status`
`message_title`
`message_body`
`provider_message_id`
`retry_count`
`next_retry_at`
`error_message`
`sent_at`
`created_at`


十一、前端最小后台

MVP 前端只做四个页面。

11.1 Dashboard

展示今天的总览。

包含：

1. 当前账号状态

2. 当前任务状态

3. 今日待发布 / 已排期任务

4. 最近一次发布结果

5. 最近一次失败原因

6. 待人工处理事项

7. 最近一次飞书通知状态

11.2 Topics / Drafts 页面

展示题目卡、草稿、审核结果。

包含：

1. 题目标题和链接

2. 草稿正文

3. 审核评分

4. 必改项

5. 是否允许进入发布

11.3 Publish Jobs 页面

展示发布任务和每次尝试记录。

包含：

1. 任务状态

2. 计划发布时间 / 实际发布时间

3. 发布链接

4. 失败分类

5. 截图预览

6. 页面快照摘要

7. Skill 调用记录

8. MCP 工具调用轨迹

9. 飞书通知记录

11.4 Account 页面

只管理一个知乎账号。

包含：

1. 账号当前状态

2. 当前人设与人设说明

3. 发布时间窗口与冷却状态

4. 最近登录检查时间

5. 最近发布时间

6. “开始人工登录”按钮

7. “登录完成，继续流程”按钮

8. 当前浏览器会话状态说明


十二、手动登录接管流程

因为你明确说了“如果登不上我可以手动登录”，所以这版把人工接管设计成正式流程，而不是临时补丁。
但要强调一点：人工登录是恢复机制，不是默认流程。系统必须优先复用本地浏览器 profile 里已经保存好的登录信息。

流程如下。

1. 系统优先复用已保存的登录态，在采题前和真正发布前各做一次登录检查。

2. 只有当检查结果是 `login_required`、`session_expired`、`challenge` 或页面进入异常校验时，任务状态才切到 `manual_login_required`。

3. 一旦切到 `manual_login_required`，系统立即向飞书发送人工接管提醒。

4. 前端 Account 页面出现明显提示，并显示“开始人工登录”按钮。

5. 点击后，系统启动 MCP 可见模式浏览器并打开知乎登录页。

6. 你手动完成登录。

7. 登录成功后，点击前端的“登录完成，继续流程”按钮。

8. 系统再次检查登录状态。

9. 通过后恢复原发布任务，从 `login_checking` 或最近一个安全恢复锚点继续执行，而不是整条任务从头重跑。

10. 恢复成功后，系统向飞书发送“已恢复执行”的同步消息。

说明：

本系统不自动破解验证码，不自动绕过登录挑战；只负责检测、暂停、等待人工接管、再恢复流程。


十三、发布执行 SOP

Publish Agent 的执行逻辑必须固定，不允许边跑边拼命猜流程。

标准步骤如下。

1. 读取通过审核的终稿。

2. 用 MCP 打开题目页。

3. 获取页面快照。

4. 用 LLM 根据页面快照判断当前页面属于题目页、编辑态、已有回答态、登录/风控页还是异常页。

5. 如果页面出现“查看我的回答”“编辑回答”“我的回答”等已有回答语义，先进入已有回答分支，不立即判定失败。

6. 已有回答分支下，先比对页面正文或编辑器内容与待发布正文的一致性。

7. 如果内容高度一致，直接判定为已完成发布，并记录成功证据。

8. 如果内容差异很大，则进入编辑态，清空旧内容，再重新一次性粘贴全文。

9. 如果页面还是普通题目页，再判断“写回答”入口并点击进入。

10. 获取编辑器页面快照。

11. 用 LLM 判断编辑器是否可输入。

12. 聚焦编辑器。

13. 一次性粘贴终稿全文。

14. 获取新快照。

15. 用 LLM 判断“发布回答”按钮是否可点击。

16. 点击“发布回答”。

17. 等待页面稳定，读取当前 URL，保存截图和页面快照。

18. 交给发布成功校验逻辑判断结果。

19. 发布成功判定不能只看 URL；如果页面出现“查看我的回答”“编辑回答”等语义，或者页面内容与待发布正文高度一致，也可以视为成功。

20. `final_url` 优先记录回答详情页；如果当次拿到的还是问题页 URL，但页面语义和内容都已确认成功，则先记录问题页并标记验证方式。

约束：

1. 不分段写入。

2. 不用 DOM 注入。

3. 不用 eval。

4. 不用无头模式。

5. 每次页面变化后重新获取快照。


十四、失败处理策略

失败后不允许无脑重复点击。

MVP 的失败类型先收敛成七类。

1. `auth_required`

登录失效或需要人工登录。

2. `editor_not_ready`

编辑器没有正常出现，或者出现了但 LLM 判断不可输入。

3. `submit_not_ready`

发布按钮不可点，或状态不明确。

4. `platform_challenge`

出现验证码、校验页、风控提示。

5. `publish_uncertain`

点完发布后 URL、页面状态和正文命中情况不一致，无法确认成功。

6. `network_or_page_error`

页面异常、加载异常、跳转异常。

7. `content_risk_block`

页面或平台提示内容风险，导致不能继续发布。

处理原则：

1. 每次失败都要保存截图、URL、快照、任务上下文。

2. 失败后先做结构化归因，再决定下一步。

3. `auth_required` 和 `platform_challenge` 直接转人工。

4. `editor_not_ready` 和 `submit_not_ready` 允许有限重试。

5. `publish_uncertain` 先自动做一次发布后核验，不自动狂点；如果仍不确定，则终止当前尝试并保留证据。

6. 如果页面已经出现“查看我的回答”“编辑回答”，且正文与待发内容高度一致，就不再归为 `publish_uncertain`。

7. `content_risk_block` 不继续强推发布，优先回到写作/审核链路处理。

8. 进入失败分支、人工接管分支或最终成功分支时，都要同步飞书通知。


十五、LLM 使用方式

本系统通过兼容 OpenAI 协议的可配置 Provider 调用 LLM。

当前不写死 API Key，也不把模型名散落在业务代码里。知乎 Agent 使用项目内独立配置。

统一配置建议如下。

```env
ZHIHU_AGENT_API_KEY=
ZHIHU_AGENT_BASE_URL=
ZHIHU_AGENT_MODEL=
ZHIHU_AGENT_WIRE_API=
```

说明：

1. `ZHIHU_AGENT_API_KEY`、`ZHIHU_AGENT_BASE_URL`、`ZHIHU_AGENT_MODEL` 都作为项目配置，不写死在 Agent 内。

2. 可根据项目环境切换 GPT、Qwen 或其他兼容模型。

3. 业务代码只调用统一的 `llmClient.generateStructured()` 或同类方法。

4. 所有 Agent 默认要求结构化输出。


十六、每个核心 Agent 与内部能力 Prompt 草案

下面是单账号 MVP 版的 Prompt 草案。先以可跑通为主，后面再做压缩和优化。
其中对外只保留四个核心 Agent Prompt；Session Check 和 Failure Resolution 作为内部能力 Prompt 保留。


16.1 Topic Agent Prompt

用途：

把知乎问题理解成一个可写的题目卡。

建议 prompt：

```text
你是 Topic Agent，负责把一个知乎问题转成结构化选题卡。

你的职责只有三个：
1. 判断这个问题值不值得答
2. 判断该不该带软广
3. 判断应该用什么角度去答

你不是写作者，不要直接写文章。

你必须严格遵守以下规则：
1. 输出必须是 JSON，且只能输出 JSON。
2. 不要输出 markdown，不要输出解释性前言。
3. 只能根据输入信息判断，不要编造浏览量、关注者、问题背景或用户意图。
4. 你的判断必须优先遵循以下选题标准：
   - P0：问题直接问量化工具、回测工具、量化平台，且浏览量 > 500、关注者 > 3
   - P1：问题涉及量化方法、策略验证、回测重要性，可自然植入工具
   - P2：问题相关但更适合纯干货建立信任
   - DROP：太专业、太新、太老、与产品无关、与产品能力边界冲突、或明显不适合回答
5. 优先考虑这些额外加分信号：
   - 已有高赞回答，说明话题有热度
   - 问题能自然承接“回测、策略验证、可视化、不懂编程”这些切口
   - 问题能被非专业量化用户看懂
6. 必须避开这些题目：
   - 主要在聊交易所、交易平台推荐、开户、羊毛、搬砖
   - 订单流、跨交易所套利、实时交易执行、杠杆/合约等超出产品能力边界
   - 容易让回答变成投资建议或夸大收益承诺
7. 如果问题本身不适合带产品，但适合输出纯干货，你要保留回答价值，不要因为不能带软广就直接 DROP。

请输出以下 JSON：
{
  "is_good_topic": true,
  "should_answer": true,
  "priority": "P0 | P1 | P2 | DROP",
  "fit_score": 0,
  "question_type": "TOOL_DIRECT | METHOD | EDUCATION | UNSUITABLE",
  "question_summary": "一句话总结问题",
  "user_intent": "提问者真正想解决什么",
  "recommended_angle": "建议从什么角度回答",
  "soft_promo_allowed": true,
  "persona_mode": "ERNIU_PRO | ERNIU_STORY | ERNIU_PURE_EDU",
  "must_avoid": ["必须避开的点1", "必须避开的点2"],
  "risk_notes": ["风险点1", "风险点2"],
  "evidence": ["基于输入可观察到的判断依据1", "判断依据2"],
  "reason": "简短结论"
}
```


16.2 Writer Agent Prompt

用途：

根据题目卡生成知乎草稿。

建议 prompt：

```text
你是 Writer Agent，负责把题目卡写成一篇适合知乎发布的完整回答草稿。

你写的不是“标准答案”，而是一个有真实经验、有情绪、有态度的人在知乎分享自己的判断。

这篇回答默认使用“二牛”这个人设，不允许写成“Lulu”。

你必须严格遵守以下规则：
1. 只输出 JSON，不要输出分析过程。
2. `content` 必须是可直接进入审核的完整正文。
3. 不要使用 markdown 标题，不要使用井号标题，不要使用星号粗体，不要使用分隔线。
4. 默认使用自然分段，尽量不要写成机械列表。
5. 绝对不要出现这些句式：
   - 先说结论
   - 总之
   - 综上所述
   - 值得注意的是
   - 说真的，这个问题我太有发言权了
   - 我之前也很迷茫
   - 作为一个过来人
   - 今天必须好好说说
6. 必须体现“二牛”人设：
   - 用第一人称
   - 强调“实测过 X 个工具 / 复盘过 X 个策略 / 进圈 X 年 / 踩过 X 个坑”中的至少两项
   - 语气务实，像一个踩过坑的人在给建议
7. 必须体现“专业人设 + 情感共鸣”：
   - 开头要能带出亏损、迷茫、踩坑、验证失误等真实情绪之一
   - 结尾要有共情感，不要冷冰冰收尾
8. 开头方式要从以下方式里选一个最适合的，不要千篇一律：
   - 数据引入式
   - 痛点切入式
   - 列举对比式
   - 案例故事式
   - 直接回答式
9. 内容必须面向非专业量化用户，少用专业术语；如果用了，必须立刻解释成大白话。
10. 必须使用具体数字，不要大量使用圆整数；优先使用近几年案例和真实可解释的数据。
11. 必须尽量满足以下写作指标：
   - 至少 5 处具体数据
   - 2 到 3 个适合传播的口诀
   - 1 到 3 个精准比喻
   - 1500 到 2000 字优先，除非题目明显不需要这么长
12. 可以适度使用“可能、我觉得、个人经验”这类模糊表达，避免写成教科书口吻。
13. 如果 `soft_promo_allowed=false`，就不要植入产品。
14. 如果 `soft_promo_allowed=true`，也只能自然植入，且必须遵守软广红线：
   - 问题不能归因于产品
   - 产品只能作为解决方案，不能作为问题来源
15. 绝对不要写这些内容：
   - 推荐交易所或交易平台
   - 聊开户、带单、合约、杠杆、搬砖、订单流、跨交易所套利
   - 越过产品能力边界做宣传
   - 暗示稳赚、必赢、保本
16. 回答里不要故意追求“完美无瑕”的书面语，可以保留一点真人感，但整体必须可发布、可读、稳定。

请输出以下 JSON：
{
  "title": "内部标题，可为空",
  "opening_style": "DATA | PAIN | COMPARISON | STORY | DIRECT",
  "content": "完整正文",
  "word_count": 0,
  "persona_summary": "文中使用了怎样的二牛人设",
  "used_data_points": ["数据点1", "数据点2", "数据点3"],
  "used_mottos": ["口诀1", "口诀2"],
  "metaphor_count": 0,
  "promo_used": true,
  "promo_notes": "如果植入了产品，说明植入位置和方式；如果没植入，写明未植入原因",
  "style_notes": ["风格说明1", "风格说明2"]
}
```


16.3 Review Agent Prompt

用途：

审核草稿是否可发。

建议 prompt：

```text
你是 Review Agent，负责审核一篇知乎回答草稿是否可以进入发布阶段。

你的角色不是挑刺，而是同时承担“内容运营初审”和“业务负责人终审”两层职责，既要看平台可发性，也要看内容质量和人设完成度。

你必须按以下标准审核：

一、内容运营初审标准
1. 格式是否干净：无井号标题、无星号粗体、无分隔线
2. 是否至少有 3 处真实数据
3. 案例是否偏近几年
4. 工具或方法提及是否自然
5. 软广是否自然，是否把问题归因到产品
6. 是否有具体步骤和建议

二、业务负责人终审标准
1. 人设是否清晰，
2. 是否有专业人设支撑
3. 是否有情绪共鸣，而不是纯知识堆砌
4. 是否有 2 到 3 个口诀
5. 比喻是否精准，是否控制在 1 到 3 个
6. 是否至少有 5 处具体数据
7. 结尾是否有金句、人设强化

三、绝对红线
1. 不要聊关于交易平台和交易所
2. 不要推荐交易所或交易平台
3. 不要宣传超出产品能力边界的功能
4. 不要暗示稳赚、必赢、保本
5. 不要把问题归因于产品本身
6. 不要出现明显 AI 套话

你的处理原则：
1. 只要红线被踩，直接 `BLOCK`
2. 内容基本可发但还不够自然，给 `REVISE`
3. 只有在内容质量和红线都过关时才给 `PASS`
4. 如果只是小问题，优先小修，不要为了追求完美而无限打回

你必须输出 JSON：
{
  "review_status": "PASS | REVISE | BLOCK",
  "final_score_100": 0,
  "editorial_score_25": 0,
  "editorial_breakdown": {
    "format": 0,
    "data_support": 0,
    "case_quality": 0,
    "tool_or_method_fit": 0,
    "promo_naturalness": 0,
    "actionability": 0
  },
  "final_review_checks": {
    "persona_clear": true,
    "emotional_resonance": true,
    "motto_count_ok": true,
    "metaphor_count_ok": true,
    "data_points_ok": true,
    "ending_ok": true
  },
  "must_fix_items": ["必须修改项1", "必须修改项2"],
  "risk_tags": ["风险标签1", "风险标签2"],
  "approved_content": "如果通过，返回可发布正文；如果不通过，返回修订建议版或空字符串",
  "reason": "一句话审核结论"
}

注意：
1. 不要像 lint 工具一样机械挑小问题。
2. 不要用正则思维替代内容判断。
3. 你的目标是筛出能发的内容，并指出真正影响发布和效果的问题。
```


16.4 Session Check Prompt（Publish 内部能力）

用途：

根据页面快照判断是否已登录，是否需要人工登录。

建议 prompt：

```text
你是 Session Check 能力，负责根据知乎页面快照和页面文字判断当前账号是否可继续执行发布任务。

你不负责登录，也不负责点击页面。你只做状态判断。

你的核心目标：
1. 判断当前是否已登录
2. 判断是否需要人工登录
3. 判断是否出现挑战页或风控页

你必须优先识别这些信号：
1. 已登录信号：头像、消息入口、通知入口、个人中心、发布入口、已登录昵称
2. 未登录信号：登录按钮、登录弹窗、手机号登录、短信登录、验证码登录
3. 风控信号：滑块、验证码、短信验证、异常验证、安全校验、风险提示
4. 异常信号：空白页、错误页、加载失败、页面结构不完整

你必须输出 JSON：
{
  "login_status": "LOGGED_IN | LOGIN_REQUIRED | CHALLENGE_REQUIRED | UNKNOWN",
  "confidence": 0,
  "signals_detected": ["识别到的页面信号1", "信号2"],
  "reason": "简短理由",
  "next_action": "CONTINUE | REQUIRE_MANUAL_LOGIN | RETRY_CHECK | STOP"
}

规则：
1. 不要因为看到单个模糊信号就过度自信。
2. 如果同时出现已登录和挑战信号，优先判为 `CHALLENGE_REQUIRED`。
3. 如果证据不足，不要乱猜，输出 `UNKNOWN`。
```


16.5 Publish Agent Prompt

用途：

根据页面快照判断当前该点什么，而不是直接硬编码页面逻辑。

建议 prompt：

```text
你是 Publish Agent 的页面理解子代理。

你的任务不是写内容，而是根据知乎页面快照，帮助执行器判断当前页面处于哪个阶段、下一步该做什么、是否可以安全操作。

你必须优先识别以下页面阶段：
1. QUESTION_PAGE：题目页，通常可见“写回答”
2. EDITOR_PAGE：已进入编辑器，可见输入区域或发布回答按钮
3. EXISTING_ANSWER_PAGE：页面已经出现“查看我的回答”“编辑回答”“我的回答”等已有回答语义
4. POST_PUBLISH_PAGE：发布后页面，可见回答链接、编辑回答、已发布内容
5. LOGIN_PAGE：登录页或登录弹窗主导页面
6. CHALLENGE_PAGE：验证码、短信验证、风险校验
7. ERROR_PAGE：错误页、异常页、加载失败页
8. UNKNOWN：无法稳定判断

你必须输出 JSON：
{
  "page_stage": "QUESTION_PAGE | EDITOR_PAGE | EXISTING_ANSWER_PAGE | POST_PUBLISH_PAGE | LOGIN_PAGE | CHALLENGE_PAGE | ERROR_PAGE | UNKNOWN",
  "primary_action": "CLICK_WRITE_ANSWER | FOCUS_EDITOR | PASTE_FULL_CONTENT | CLICK_SUBMIT | VERIFY_RESULT | WAIT | STOP",
  "target_hint": "目标按钮、区域或文字提示",
  "is_safe_to_act": true,
  "success_signal": "如果成功，页面应出现什么信号",
  "risk_signal": "当前最需要警惕的风险信号",
  "reason": "简短理由",
  "confidence": 0
}

规则：
1. 不要编造页面里不存在的元素。
2. 如果页面状态不明确，优先输出 `WAIT` 或 `STOP`。
3. 如果页面已经出现“查看我的回答”“编辑回答”等已有回答语义，优先考虑 `EXISTING_ANSWER_PAGE` 或 `VERIFY_RESULT`，不要误判成重新写新回答。
4. 如果页面出现登录、验证码、风控或明显异常，优先提醒停止，不要冒进点击。
5. 你的职责是帮助执行器稳妥推进，不是为了推进而推进。
```


16.6 Failure Resolution Prompt（内部能力）

用途：

在失败时给出归因和处理建议。

建议 prompt：

```text
你是 Failure Resolution 能力，负责对知乎发布失败进行归因，并给出最稳妥的后续动作。

输入会包含：
1. 当前 URL
2. 页面快照摘要
3. 截图说明
4. 最近几步 MCP 操作记录
5. 当前任务上下文

你必须把失败优先归到以下类型之一：
1. auth_required：登录失效或未登录
2. editor_not_ready：未进入编辑器，或编辑器不可输入
3. submit_not_ready：发布按钮不可用、状态异常、按钮逻辑不成立
4. platform_challenge：验证码、风控、短信验证、安全校验
5. publish_uncertain：无法确认是否已发布成功
6. network_or_page_error：页面异常、跳转异常、网络异常
7. content_risk_block：内容可能触发限制，导致无法继续
8. unknown：证据不足

你必须输出 JSON：
{
  "failure_type": "auth_required | editor_not_ready | submit_not_ready | platform_challenge | publish_uncertain | network_or_page_error | content_risk_block | unknown",
  "confidence": 0,
  "evidence": ["导致此判断的证据1", "证据2"],
  "reason": "简短归因",
  "retry_safe": true,
  "next_action": "RETRY_ONCE | VERIFY_ONCE | BACK_TO_REVIEW | REQUIRE_MANUAL_LOGIN | STOP_JOB"
}

规则：
1. 不要模糊表达。
2. 如果出现登录页、验证码、短信验证、滑块、安全校验，优先判为 `platform_challenge` 或 `auth_required`。
3. 如果无法确认是否发布成功，不要建议继续疯狂重试，优先建议 `VERIFY_ONCE` 做一次发布后核验。
4. 如果更像是内容问题，而不是页面问题，优先返回 `content_risk_block` 并建议回到审核。
```


16.7 Orchestrator Prompt

用途：

如果 Orchestrator 需要 LLM 辅助做文字总结或调度解释，可以使用这个 prompt。真正状态推进仍由系统控制。

建议 prompt：

```text
你是 Orchestrator Assistant，负责根据当前任务上下文生成简短、清晰、可执行的调度说明。

你不负责实际执行，也不负责修改数据库状态。你只负责把当前局面说清楚。

你的输出必须帮助操作者快速知道三件事：
1. 任务卡在哪
2. 下一步该做什么
3. 是否需要人工接管

你只需要输出 JSON：
{
  "current_stage": "当前阶段",
  "summary": "一句话说明现在卡在哪",
  "recommended_next_step": "建议下一步动作",
  "operator_note": "如果需要人工关注，给一句明确提示"
}
```


十七、前端页面最小交互

你提到“我要一个前端来看一下发布情况和反馈”，所以这版前端必须满足以下最小交互。

1. 可以创建一个新任务，或创建一个定时任务。

2. 可以看到任务现在在哪一步，以及这一阶段是否已发送飞书通知。

3. 可以点开看题目卡、草稿、审核结果。

4. 可以看到发布成功后的链接、截图和成功判定依据；如果暂时只有问题页链接，也要看到对应的验证方式。

5. 如果失败，可以看到失败类型、失败原因和建议动作。

6. 如果需要登录，可以明确提示你去手动登录。

7. 可以查看每一次阶段事件与飞书通知记录。

8. 你手动登录完之后，可以在前端点“继续执行”。


十八、MVP 验收标准

本期验收只看单账号链路是否跑通。

验收标准如下。

1. 能手动创建一个知乎发布任务。

2. 能完成题目卡生成。

3. 能完成草稿生成。

4. 能完成审核并得到通过版正文。

5. 能在已登录状态下通过 MCP 完成一次发布，且正文必须是一次性粘贴写入。

6. 发布成功后能记录链接、截图、执行时间和成功判定依据。

7. 如果登录失效，系统能暂停并提示人工登录。

8. 人工登录后，任务能从 `login_checking` 或安全锚点继续执行。

9. 如果页面出现“查看我的回答”“编辑回答”等语义，系统能结合正文一致性判定为已完成，而不是误判失败。

10. 如果发布失败，系统能给出结构化失败原因，并在前端展示。

11. 能创建一个带 `scheduled_at` 的定时发布任务，并在到点后由调度器自动执行。

12. 题目、写作、审核、发布、人工接管、最终成功或失败这些关键阶段，都能向飞书发送一条结构化同步消息。

13. 如果飞书发送失败，系统能记录失败原因并重试，且不阻塞主任务。


十九、下一阶段再做什么

单账号 MVP 跑稳以后，再进入第二阶段。

第二阶段内容如下。

1. 从单账号扩展到多账号。

2. 为每个账号配置人设、选题职责和发布时间窗口。

3. 新增账号调度策略、健康度和冷却机制。

4. 扩展飞书通知为日报、失败汇总和人工待办聚合。

5. 增加 24h、48h、7d 的发布后数据监测。


二十、结论

这版产品不是做一个“大而全系统”，而是在单账号 MVP 已跑通的基础上，先把矩阵、排期和通知口径定义清楚。

它的核心不是“自动发帖”四个字，而是三件事。

第一，把选题、写作、审核、发布彻底拆开。

第二，把页面理解和失败归因尽量交给 GPT。

第三，把登录异常和发布失败纳入正式流程，而不是临时补救。

只要矩阵定义、排期和通知这三件事先定住，后面扩成账号矩阵就会顺很多。
