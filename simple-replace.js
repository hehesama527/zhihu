const fs = require('fs');
let content = fs.readFileSync('h:/claw/apps/api/src/server.ts', 'utf8');

// ????????
const wrong1 = '???????????????';
const correct1 = '?????????';

const wrong2 = '???????????????????????';
const correct2 = '?????????';

content = content.split(wrong1).join(correct1);
content = content.split(wrong2).join(correct2);

fs.writeFileSync('h:/claw/apps/api/src/server.ts', content, 'utf8');
console.log('Done!');
