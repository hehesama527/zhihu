# 知乎账号矩阵化改造实施指南

版本日期：2026-03-27

配套主文档：`知乎账号矩阵自动化发布系统-产品文档.md`

## 一、这份指南的用途

这份文档只保留当前测试阶段真正需要执行的口径，不再展开长篇说明。

一句话：

`矩阵 = 多账号 + 独立登录环境 + 独立选题/写作视角 + 统一审核/发布目标 + 账号级排期`

## 二、当前已经完成的内容

本轮已经落地的重点只有这些：

1. 系统不再假设只有单账号。
2. 每个账号有自己的浏览器 Profile。
3. 发布完成后会关闭本次运行时浏览器，但保留 Profile 和登录态。
4. Writer Prompt 已支持按账号绑定。
5. job 创建时会自动固化 Prompt 快照。
6. 每个账号每天独立生成 3 到 4 个排期时间点。
7. 账号的人设差异当前先走轻量方案，不要求重写整套 Prompt。
8. 飞书通知已纳入当前最小范围，但只处理“问题通知”和“发布成功通知”。

## 三、矩阵账号登录方案

矩阵场景下，登录不能按“每次任务重新登录”设计，而要按“每个账号维护自己的长期登录资产”设计。

当前口径如下：

1. 每个账号固定一个独立浏览器用户数据目录，可理解为“一个账号一个浏览器沙箱”，不在同一浏览器实例里来回切 Cookie。
2. 每个账号首次只需要人工登录一次，登录信息保存在该账号自己的 Profile 里。
3. 后续采题、写作、审核、发布都复用这个账号自己的 Profile。
4. 发布结束后关闭浏览器窗口，但绝不删除 Profile。
5. 系统只在检测到该账号登录态失效时，才要求重新登录。
6. 某个账号登录失效，只阻塞该账号，不阻塞整个矩阵。
7. 人工登录完成后，系统会先关闭手动登录窗口，再由自动执行流程重新接管同一个 Profile；这一步如果页面短暂空白，不等于登录失败。

测试阶段建议这样使用：

1. 先把矩阵里的账号全部创建好。
2. 每个账号各做一次初始化登录。
3. 登录完成后，再开始自动排期和自动发布。
4. 未完成初始化登录的账号，不要直接放进自动运行。

补充说明：

这里说的“独立 Profile”不是“同一套浏览器里临时切换 Cookie”。
当前实现是每个账号各自持有独立 `user-data-dir`，Cookie、Local Storage、缓存、登录态都跟着该账号自己的目录走。
这比单浏览器多账号切换更稳，也更符合“一个账号一套长期登录资产”的设计。

这套方案的目标不是“永远不需要重新登录”，而是：

`正常情况下每个账号只需首次登录一次，之后长期复用；只有真实失效时才重新登录。`

## 四、自动发布失败原因与解决方案

这一节不是拍脑袋猜测，来源是三部分：

1. 当前代码里已经明确落地的失败类型、恢复分支和状态流转。
2. 你这次实际遇到的登录恢复报错，已经可以确认属于 Profile 占用/浏览器上下文冲突类问题。
3. 现阶段数据库里的历史失败样本已经被清空过，所以“现网频次”暂时还不能下结论；下一轮真实测试要继续用 `publish_attempts`、`tool_traces`、截图来补证据。

同时，这一节和“三、矩阵账号登录方案”不冲突，反而是配套关系：

1. 第三节定义的是正常路径：每个账号长期复用自己的登录资产。
2. 第四节定义的是异常路径：当登录失效、Profile 被占用、页面结构变化、发布结果不确定时，系统应该如何恢复。

当前自动发布失败，主要看下面几类。

### 4.1 登录态失效

表现：

1. 页面要求登录。
2. 出现验证码、挑战页、风控页。
3. 系统提示需要人工恢复登录。

处理：

1. 只对当前账号执行人工登录恢复。
2. 恢复成功后，系统从断点继续。
3. 其他账号继续运行。

### 4.2 浏览器 Profile 被占用

这一类不是猜测，已经和你实际报错对上了。
你之前出现的 `browserType.launchPersistentContext: Target page, context or browser has been closed`，结合启动参数里的 `--user-data-dir=H:\\claw\\data\\profiles\\account-2\\msedge`，可以确认问题核心不是“你没登录成功”，而是恢复检查时 Playwright 需要重新接管这个账号的持久化 Profile，但该 Profile 当时仍被别的 Edge 进程占用或刚关闭未释放完成。

表现：

1. 你已经登录成功，但点击“继续下一步”仍失败。
2. Playwright 启动持久化浏览器时报错。
3. 本质是同一个账号的 Profile 还被别的浏览器进程占着。
4. “点击继续后跳空白页”本身不是失败信号，真正的失败点通常发生在自动流程重新接管该 Profile 的时刻。

处理：

1. 先关闭该账号对应的手动登录浏览器窗口。
2. 再执行恢复检查。
3. 如果仍失败，再重新打开该账号的手动登录窗口。

当前系统已经做了兜底清理，但原则仍然是：

`同一账号同一时刻只允许一个浏览器上下文占用它的 Profile。`

### 4.3 找不到“写回答”入口或编辑器

表现：

1. 页面结构变了。
2. 已进入问题页，但系统找不到写回答入口。
3. 已进入编辑页，但系统找不到可输入区域。

处理：

1. 同会话先重试一次。
2. 仍失败则重启浏览器再试一次。
3. 如果还失败，就看截图和 trace，补页面识别规则。

### 4.4 找不到“发布回答”按钮

表现：

1. 内容已经粘贴完成。
2. 页面没有识别到可提交按钮。
3. 或按钮状态不对，无法点击。

处理：

1. 先重试。
2. 再重启浏览器。
3. 如果持续出现，优先检查知乎页面结构是否变化。

### 4.5 发布结果不确定

表现：

1. 系统无法确认到底有没有真正发布成功。
2. 页面可能已经跳转，但证据不足。
3. 或页面上已有回答，系统无法确认是不是本次内容。

处理：

1. 系统会先做一次额外校验。
2. 如果能从已有页面验证成功，则直接补记为已发布。
3. 如果仍无法确认，再人工核对。

### 4.6 内容风控或软广风险

表现：

1. 平台提示内容风险。
2. 审核拦截。
3. 发布后结果判断为内容风险。

处理：

1. 先允许重写一次。
2. 重写后仍失败，则终止该任务。
3. 后续优先调 Writer Prompt 和 Review Prompt，不要靠硬编码规则修。

### 4.7 数据或页面异常

表现：

1. 题目信息不完整。
2. 页面加载异常。
3. 浏览器上下文异常关闭。

处理：

1. 先归类到页面/网络异常。
2. 优先看 job 的截图、tool traces、publish attempts。
3. 先判断是数据问题，还是浏览器问题，再决定修复方向。

## 五、飞书通知最小方案

这次飞书先不做全量通知系统，只做最小可用版本。

目标只有两个：

1. 每次遇到需要你介入的问题时，发飞书消息。
2. 发布成功时，发飞书消息。

当前执行口径如下：

### 5.1 问题通知触发范围

问题通知不是“所有异常都发”，而是“需要你看、需要你介入的才发”。

当前建议触发这些事件：

1. 任务进入 `manual_login_required`。
2. 任务进入 `failed_terminal`。
3. 账号登录态失效，进入 `session_expired`。
4. 检测到 `account_identity_mismatch`，也就是当前 Profile 登录的不是目标账号。
5. 发布结果在自动核验后仍然无法确认，最终落到 `publish_uncertain` 需要人工判断。

当前先不发这些：

1. 自动重试中的中间失败。
2. 同一条任务短时间内的重复重试噪声。
3. 普通流程推进状态。

### 5.2 发布成功通知触发范围

当前只在任务最终进入 `published` 时发送一次成功通知。

不要在下面这些时点提前发：

1. 仅仅点击了发布按钮但还没核验完成。
2. 只是页面疑似跳转成功。
3. 还处于 `publish_verify` 过程中。

### 5.3 飞书消息最小字段

问题通知至少带这些信息：

1. 账号名
2. 任务 ID
3. 当前阶段
4. 失败类型
5. 失败原因
6. 问题标题

发布成功通知至少带这些信息：

1. 账号名
2. 任务 ID
3. 问题标题
4. 发布时间
5. 最终链接

### 5.4 当前接入方式

当前先走飞书群机器人 `Webhook` 方案，不做更重的应用消息方案。

最小配置只有两个：

1. `FEISHU_BOT_WEBHOOK_URL`
2. `FEISHU_BOT_SECRET`，如果机器人没有开启签名校验则可为空

默认先不做：

1. `@人`
2. 日报
3. 失败汇总
4. 待办聚合
5. 复杂卡片编排

### 5.5 测试阶段怎么验证飞书

飞书通知接入后，测试阶段建议单独验证下面三件事：

1. 人工登录阻塞时，飞书能及时收到问题通知。
2. 发布成功后，飞书能收到成功通知并带上最终链接。
3. 飞书发送失败时，不影响主任务流继续写库和收证据链。

## 六、正式测试前仍未闭环的事项

到目前为止，还不能说“只剩文档里的几条问题，已经全部收口了”。
正式测试前，至少还有下面这些点需要明确处理。

### 5.1 账号初始化还没完全完成

当前矩阵账号在设计上已经成立，但运行态上还没有完全初始化完。

现阶段要关注两件事：

1. 并不是所有账号都已经完成首次人工登录。
2. 未完成初始化登录的账号，不应该直接进入自动排期和自动发布。

判断标准不要只看账号有没有创建出来，而要看：

1. 账号状态是否已经恢复到 `active`。
2. 该账号对应的 Profile 目录是否已经真实落到磁盘。
3. 该账号是否已经用自己的 Profile 完成过一次人工登录确认。

### 5.2 Profile 根路径当前不一致

这不是抽象风险，而是当前运行态里已经存在的历史遗留问题。

当前至少有一类账号仍然沿用了早期单账号阶段的 Profile 目录，例如：

1. 某些历史账号的 `profile_dir` 仍指向 `H:\\claw\\apps\\api\\data\\profiles\\account-x`。
2. 当前矩阵口径下，新的标准目录应该统一到 `H:\\claw\\data\\profiles\\account-x`。

这类不一致短期内不一定立刻报错，但正式测试前最好统一。
原因很简单：

1. 后面排查登录复用时会混淆到底哪个目录才是当前账号的真实登录资产。
2. 后面清理浏览器占用时，也容易误关、误判或者清理不到位。
3. 历史默认账号如果沿用了老目录，更容易出现“账号资料名称”和“真实登录态”不一致的问题。

当前系统已经补上了账号身份一致性校验，也会在账号页直接提示历史 Profile 风险。
但这只能拦住错号继续执行，不能代替目录统一本身。

### 5.3 测试数据目前不是干净态

当前库里仍然可能保留旧排期和旧任务，而发布证据链又没有同步留下来。
这种状态下直接正式测试，结果会掺杂历史脏数据，归因会不干净。

重点不是“库里有没有数据”，而是下面这组关系是否一致：

1. `publish_jobs` 和 `daily_publish_schedule` 是否还残留上一次测试的旧记录。
2. `publish_attempts`、`tool_traces`、截图、artifact 是否和这些任务一一对应。
3. 当前看到的失败或阻塞，究竟是新测试产生的，还是旧任务遗留下来的。

如果旧任务还在，但 `publish_attempts`、`tool_traces` 已经被清空或没有对应内容，那么这批数据就不适合继续作为正式测试基线。

### 5.4 自动发布链路还没有完成最新一轮真实闭环

现在可以确认的是：

1. 代码层面的失败分支和恢复逻辑是存在的。
2. `Profile 被占用` 这一类问题，已经通过真实报错验证过。
3. 登录身份一致性校验已经补上，可以拦住“当前 Profile 登录的是别的账号”这种情况。

但还不能直接说“自动发布链路已经全部验完”。
原因是下面这些情况，目前更多属于“代码已覆盖”，不是“最新一轮真实发布已经全部打过证据链”：

1. 找不到编辑器。
2. 找不到发布按钮。
3. 发布结果不确定。
4. 内容风控或软广风险。

要真正把这部分收口，还是得跑一轮新的真实发布，把下面这些证据重新打出来：

1. `publish_attempts`
2. `tool_traces`
3. 截图和 artifact
4. job 的 `lastErrorType`、`failureReason`、恢复动作流转

没有这轮最新证据链，就不能负责任地说“所有问题都找完了”。

## 七、当前测试阶段的执行建议

为了让矩阵测试更稳，当前建议按下面顺序执行：

1. 先停 worker，再清空旧测试数据。
2. 统一所有账号的 `profile_dir` 根路径，尤其先处理历史默认账号这类沿用旧目录的账号。
3. 创建矩阵账号，并确认每个账号都有自己的 Profile。
4. 每个账号各做一次初始化登录，确认全部进入可运行状态。
5. 先用单账号跑通一次完整自动发布。
6. 确认 `publish_attempts`、`tool_traces`、截图、artifact 都已经正常留下。
7. 单独验证一轮飞书问题通知和发布成功通知。
8. 再增加到双账号矩阵测试。
9. 最后再扩大到更多账号。

不要一上来就多账号并发猛跑，先验证这三件事：

1. 账号隔离是否稳定。
2. 登录复用是否稳定。
3. 发布失败归因是否清楚。

## 八、这一轮的最终结论

本轮矩阵化改造，真正重要的不是“并发”本身，而是先把下面四件事做稳：

1. 每个账号都有独立 Profile。
2. 每个账号的登录态可以长期复用，不需要每次重登。
3. 每个账号可以绑定自己的 Writer Prompt。
4. job 在创建时会固化自己的 Prompt 快照。

等这四件事稳定之后，再往上叠：

1. 飞书日报、失败汇总和待办聚合。
2. 更细的人设配置。
3. 更完整的矩阵选题分发。
4. 真正的多账号并发调度。
