export default function TwitterPublishPage() {
  return (
    <div className="stack">
      <section className="page-header">
        <div>
          <h2>X Publish</h2>
          <p className="muted">发布页不再展示 research 相关状态。任务只会走 planned → writing → review → publish 这条链路。</p>
        </div>
      </section>
    </div>
  );
}