单账号知乎自动化 MVP 工作流最终口径

版本日期：2026-03-24

这份文档用于冻结单账号 MVP 的正确顺序。后续代码、前端状态、Agent Prompt、skill 和 MCP 调用都以这一版为准。

一、任务创建与状态机起点

前端手动创建任务，或者调度器生成当天排期后创建任务时，API 必须先写入 `publish_jobs` 占位记录。

这条规则的意思是：
1. job 一旦创建，前端立刻查得到
2. job 的后续推进由 Orchestrator 或 Worker 完成
3. 不能只返回 `accepted`，却不先落库

也就是说，正确顺序是：

前端或调度器创建 job

API 先落库 `publish_jobs`

Orchestrator 再推进后续状态

二、单账号 MVP 正确主链

单条任务的标准顺序固定如下：

调度器或前端创建 job

Orchestrator 取到 job

如果候选池不足，先做一次登录检查，再执行 Topic Discovery

Topic Agent 生成题目卡

Topic Review 在 Topic 阶段内做选题重复性拦截

Writer Agent 生成草稿

调用 `humanizer-zh` 做去 AI 味处理

Review Agent 执行 `Hard Gate -> Editorial Review -> Publish Review`

审核通过后进入 `review_passed`

真正发布前再做一次登录检查

Publish Agent 基于页面快照决策下一步动作

通过 `playwright` MCP Runtime 执行动作

发布后核验

如失败则进入 Failure Resolution

如需要人工介入，只允许进入 Manual Login Recovery

补充说明：
1. 登录检查是 Publish 阶段的内部能力，不单独算一个对外主 Agent
2. Failure Resolution 是 Orchestrator 内部恢复能力，不单独算一个对外主 Agent
3. 发布成功不能只看 URL，还要结合页面语义和正文一致性判断
4. 如果页面出现“查看我的回答”“编辑回答”“我的回答”等已有回答语义，必须优先进入已有回答分支，而不是误判为失败

三、登录策略最终口径

登录不是默认人工步骤。

系统应当优先复用本地浏览器 profile 中已经保存好的登录信息。只有出现以下情况时，才进入人工登录恢复：

1. `login_required`
2. `session_expired`
3. 命中登录挑战、异常验证、风控页

人工登录的定位是“恢复机制”，不是“常规流程”。

另外，登录检查不应该只放在发布前，也不应该把所有 LLM 步骤都卡住。正确做法是：

1. 涉及知乎页面采集前，先做 Session Check
2. 真正发布前，再做一次 Session Check
3. 纯文本链路如 Topic Agent、Writer、Review，不依赖登录态

人工登录完成后，任务应从 `login_checking` 或最近安全锚点继续，而不是整条 job 从头重跑。

四、Agent、skill、MCP 的职责边界

MVP 的边界必须固定，不允许继续混写。

对外只保留四个核心 Agent，负责理解、判断、决策。包含：
1. `Topic Agent`
2. `Writer Agent`
3. `Review Agent`
4. `Publish Agent`

其中以下能力作为内部能力保留，不再作为对外主 Agent 口径：
1. `Topic Review`
2. `Session Check`
3. `Failure Resolution`

skill 层只保留两个：
1. `humanizer-zh`
2. 浏览器 skill 适配层

其中：

`humanizer-zh` 只负责语言自然化，不负责事实补写，不负责越过能力边界，不负责改产品定位。

浏览器 skill 不负责理解页面，只负责把 Agent 已经决定好的动作请求转给 `playwright` MCP Runtime。

MCP 或 Tool Runtime 层只保留一个工具：

`playwright`

因此，正确调用关系是：

Agent 决策

如需文本自然化，调用 `humanizer-zh`

如需浏览器动作，调用浏览器 skill

浏览器 skill 再调用 `playwright` MCP Runtime

`playwright` Runtime 最终驱动可见浏览器会话

不允许业务服务直接绕过这层边界，把页面逻辑写死在业务代码里。

五、Playwright MCP Runtime 最终约束

`playwright` Runtime 只允许暴露以下动作：
1. `open`
2. `snapshot`
3. `click`
4. `focus`
5. `paste_text`
6. `type`
7. `press`
8. `wait`
9. `get_url`
10. `screenshot`

固定约束如下：
1. 只用可见模式
2. 浏览器 channel 保持可配置，当前运行环境允许 Edge
3. 不用无头模式
4. 不做验证码绕过
5. 不做 DOM 注入
6. 不做 `eval`
7. 页面发生明显变化后，优先重新 `snapshot`

核心原则只有一句话：

Agent 看 `snapshot` 决策，Runtime 只执行动作。

六、LLM 与硬规则的分工

系统仍然允许少量硬规则存在，但用途只能是兜底，不应该反过来成为主逻辑。

正确分工如下：
1. 页面理解优先交给 LLM
2. 登录态判断优先交给 Publish 阶段内的 `Session Check`
3. 发布页理解优先交给 `Publish Agent`
4. 失败归因优先交给内部 `Failure Resolution`
5. 关键词、按钮词、页面提示词只能做兜底，不应成为主要判断依据

换句话说，未来如果页面有轻微变动，系统应该优先依赖页面快照语义理解，而不是大量脆弱的字符串匹配。
但当页面语义已经非常明确时，允许保留一层轻量兜底，例如：
1. 出现“写回答”时兜底为 `CLICK_WRITE_ANSWER`
2. 出现明显编辑器工具栏或空编辑区时兜底为 `FOCUS_EDITOR`
3. 出现“查看我的回答”“编辑回答”时兜底进入已有回答核验
4. 出现“发布回答”时兜底为 `CLICK_SUBMIT`

七、Review Agent 最终判定规则

Review Agent 固定三段：

`Hard Gate -> Editorial Review -> Publish Review`

最终判定只允许四种：
1. `BLOCK`
2. `BLOCK_DUPLICATION`
3. `REVISE`
4. `PASS`

固定规则如下：
1. 红线问题命中，直接 `BLOCK`
2. 内容重复命中，直接 `BLOCK_DUPLICATION`
3. 可修订问题，返回 `REVISE`
4. 全部通过，返回 `PASS`

特别强调：

如果 `Editorial Review` 判定为 `REVISE`，就不能偷偷被降级成 `PASS`。只要还存在需要修订的问题，就不应该直接进入发布。

八、重复性问题最终处理方式

重复性问题不是文风问题，而是策略问题。

因此处理规则固定如下：

选题重复：

直接拦截当前题目

清理候选池中的潜在同类题

重新选题

内容重复：

直接终止当前稿件

不进入文案微调回路

回到选题池重新走写作链路

也就是说，重复性问题一律不通过改一改再发来解决。

九、失败恢复最终口径

Failure Resolution 的动作固定为：
1. `RETRY_SAME_SESSION`
2. `RESTART_BROWSER`
3. `VERIFY_ONCE`
4. `REWRITE_ONCE`
5. `MANUAL_LOGIN`
6. `RESELECT_TOPIC`
7. `TERMINAL_FAIL`

恢复原则固定如下：
1. 登录问题走 `MANUAL_LOGIN`
2. 重复性问题走 `RESELECT_TOPIC`
3. 浏览器轻量错误先同会话重试
4. 页面或网络异常可重启浏览器
5. 发布不确定先做一次核验
6. 内容风险拦截允许回 Writer 重写 1 次
7. 超出规则上限后终止

补充规则：
1. 如果页面已经出现“查看我的回答”“编辑回答”，并且正文与待发内容高度一致，就不再进入 `publish_uncertain`
2. 发布成功归档时，`final_url` 优先记录回答详情页
3. 如果当次只能确认问题页 URL，但页面语义和正文已确认成功，也允许先记问题页 URL，同时额外记录验证方式，后续再补强 permalink 归档

十、当前代码改造优先级

为了让实现重新回到本文件的口径，下一步改造优先级固定如下：

第一优先级：把发布成功后的最终链接归档补全，优先拿到回答详情页 permalink，并为问题页兜底成功补 `final_url_type`

第二优先级：修正 `publish_attempts` 的 attempt 编号与“最新尝试”判定，避免手动重跑后前端仍显示旧失败记录

第三优先级：继续把 Publish 与登录检查链路收敛到 Agent + skill + MCP Runtime 的边界内，避免业务层再写死页面判断

第四优先级：减少页面判断中的硬编码字符串，把主逻辑交还给 LLM 快照理解，同时只保留必要的明显状态兜底
