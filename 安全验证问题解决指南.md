# 知乎安全验证问题解决指南

## 问题现象
飞书通知显示多个账号发布失败：
- 当前阶段：`failed_terminal`
- 触发步骤：`writer`
- 原因：`Connection error.`

## 根本原因
账号触发知乎风控，进入安全验证页面：
`https://www.zhihu.com/account/unhuman`

## 解决方案

### 方案 1：人工完成验证（推荐）

1. **手动打开浏览器，登录对应账号**
   ```
   账号 1: 二牛是个老实人
   账号 2: 焦花花
   ```

2. **完成安全验证**
   - 访问 https://www.zhihu.com/
   - 按提示完成滑块/短信验证

3. **验证后保持浏览器打开 1-2 分钟**
   - 让 Cookie 充分更新
   - 浏览几个正常页面

4. **重启服务**
   ```bash
   # 如果使用 PM2
   pm2 restart all
   
   # 或直接重启 worker
   npm run dev:backend
   ```

### 方案 2：重置 Profile（如方案 1 无效）

如果验证后仍反复出现，可能需要重置账号 Profile：

1. **停止服务**
   ```bash
   pm2 stop all
   ```

2. **备份并删除 Profile**
   ```bash
   # 备份（可选）
   xcopy /E /I data\profiles\account-1 data\profiles\account-1.backup
   xcopy /E /I data\profiles\account-2 data\profiles\account-2.backup
   
   # 删除 msedge 目录
   rmdir /S /Q data\profiles\account-1\msedge
   rmdir /S /Q data\profiles\account-2\msedge
   ```

3. **重新启动服务并手动登录**
   ```bash
   pm2 start all
   ```
   
   然后在 Web 界面触发手动登录流程

### 方案 3：检查网络环境

如果多个账号频繁触发风控，检查：

1. **IP 地址**
   - 是否使用数据中心 IP
   - 是否多人共享同一 IP
   - 考虑切换家庭宽带 IP

2. **发布频率**
   - 降低发布频率
   - 增加发布时间间隔
   ```
   修改 .env 中的 WORKER_INTERVAL_MS（默认 45000ms）
   建议调整为 90000-180000ms（1.5-3 分钟）
   ```

3. **账号行为**
   - 避免短时间内大量发布
   - 增加正常浏览行为
   - 保持账号日常活跃

## 预防措施

### 1. 调整发布策略
```env
# .env
WORKER_INTERVAL_MS=120000  # 增加到 2 分钟
```

### 2. 添加发布前检查
在 Web 界面添加"账号健康检查"功能，定期检查：
- 账号是否处于验证状态
- Cookie 是否有效
- 能否正常访问发布页面

### 3. 监控告警优化
修改飞书通知，区分：
- 真正的网络错误
- 安全验证/风控错误
- 登录态过期

## 紧急恢复步骤

1. **立即停止自动化发布**
   ```bash
   pm2 stop worker
   ```

2. **人工登录所有账号完成验证**

3. **等待 30 分钟 -1 小时**
   - 让知乎风控降级
   - 账号恢复正常权重

4. **降低频率后重启**
   ```bash
   # 修改 .env
   WORKER_INTERVAL_MS=180000  # 3 分钟间隔
   
   # 重启
   pm2 restart all
   ```

## 联系支持

如问题持续，请收集以下信息：
1. `data/logs/` 下的最新日志
2. 安全验证页面的截图
3. 账号最近 7 天的发布记录
