# 矩阵辅助配图子系统 - 产品文档

产品文档 v1.1 | 2026-04-12 | 负责人：二牛

---

## 一、定位

把已有表情包、梗图、场景图、截图统一资产化，知乎和 X 共用一套图库，按关键词、OCR、标签、平台范围快速找图。**本版不展开生图能力**。

---

## 二、功能实现进度

### ✅ 已完成

| 功能 | 实现位置 |
| --- | --- |
| 首页"矩阵辅助 > 配图中心"入口 | `apps/web/src/app/page.tsx` |
| 图片列表、搜索、筛选、详情、编辑 | `/images` 路由 + `ImageAssetExplorer` |
| 本地目录导入（递归扫描） | `ImageAssetImportService` |
| SHA256 hash 去重 | 入库前查 `file_hash` |
| 缩略图生成（480px WebP） | Sharp 处理链路 |
| OCR 文字提取 | Ollama 视觉模型 + OCR 补救流程 |
| 自动描述和标签 | `ImageAssetAnalysisService` |
| 人工修正（平台、类型、标签、风险） | `manualOverrideFields` 保护机制 |
| 使用记录追踪 | `image_asset_usage_records` 表 |
| 配图候选接口 | `POST /image-assets/suggest` |

### ⏸️ 暂不做

- 生图链路
- 向量数据库主链路（预留增强能力）
- 全自动无审核发布
- 图片编辑器

---

## 三、数据设计

### 表结构（已落地）

| 表 | 说明 |
| --- | --- |
| `image_assets` | 主表：元数据、OCR、标签、风险、状态 |
| `image_import_jobs` | 批量导入任务 |
| `image_asset_usage_records` | 使用记录 |

### 文件目录

```
data/image-assets/
├── originals/      # 原图
├── thumbnails/     # 缩略图
├── imports/        # 临时导入
└── quarantine/     # 风险隔离
```

---

## 四、核心流程

### 入库流程

```
目录导入 → hash去重 → 存原图 → 生成缩略图 → 写MySQL → 异步分析(OCR+Ollama)
```

自动分流规则：
- `analysis_status=completed` 且 `risk_level≠high` → 自动 `active`
- `failed/skipped/high` → 进入 `pending_review`

### 检索流程

MySQL 字段检索（第一版），支持：
- 关键词匹配：文件名、OCR、锚点词、描述、标签
- 过滤条件：平台、类型、含字、横竖版、风险、状态

### 使用流程

任务侧保存 `asset_id` → 发布时按 ID 解析路径 → 写入使用记录 → 更新 `use_count`

---

## 五、接口清单（已实现）

| 接口 | 说明 |
| --- | --- |
| `GET /image-assets` | 列表 + 篩选 |
| `GET /image-assets/:id` | 详情 + 使用记录 |
| `PATCH /image-assets/:id` | 更新信息 |
| `POST /image-assets/import-jobs` | 创建导入任务 |
| `GET /image-assets/import-jobs/:id` | 查询进度 |
| `POST /image-assets/:id/analyze` | 重跑分析 |
| `POST /image-assets/suggest` | 配图候选（供知乎/X调用） |
| `POST /image-assets/:id/usage-records` | 记录使用 |
| `GET /image-assets/:id/thumbnail` | 缩略图文件 |
| `GET /image-assets/:id/original` | 原图文件 |

---

## 六、前端页面（已实现）

| 路由 | 页面 |
| --- | --- |
| `/images` | 图片检索首页 |
| `/images/[id]` | 详情编辑 |
| `/images/import` | 目录导入 |
| `/images/review` | 待审核 |
| `/images/usage` | 使用记录 |
| `/images/library` | 图片库 |

---

## 七、研发任务拆分

### 第一批：资产底座 ✅

建表 → 文件目录 → hash去重 → 缩略图 → 列表/详情接口

### 第二批：OCR + 自动标注 ✅

视觉模型OCR → Ollama分析 → 标签写入 → 失败重跑 → 人工字段保护

### 第三批：前端管理 ✅

首页入口 → 检索页 → 导入页 → 待审核页 → 详情编辑

### 第四批：知乎/X 调用 ✅

suggest接口 → usage-records → TaskImageBinder绑定 → asset_id引用机制

---

## 八、验收标准 ✅

全部 15 项验收通过：
- 入口可见、批量导入、去重生效
- 缩略图/尺寸/hash齐全、OCR覆盖中文
- Ollama不可用不阻塞、多条件搜索、锚点词/IP搜索
- 标签分数可见、低置信度不强制写入、风险过滤
- 人工修改保护、使用记录、asset_id引用、自动分流

---

## 九、风险控制

| 风险 | 控制 |
| --- | --- |
| 标签混乱 | 分类存储 + confidence/importance 双分数 |
| OCR不准 | 人工可改 + 多轮补救 + 失败可重跑 |
| 路径迁移 | 业务只存 asset_id，路径由资产层管理 |

---

**结论**：第一阶段研发已完成，进入验收优化期。后续可接入向量检索增强和生图能力。