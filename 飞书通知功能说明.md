# 飞书通知功能说明

版本：v1.0

文档日期：2026-03-27

适用项目：知乎账号矩阵自动化发布系统

配套文档：

1. `知乎账号矩阵自动化发布系统-产品文档.md`
2. `知乎账号矩阵化改造实施指南.md`


## 一、这份文档的用途

这份文档只负责飞书通知功能本身，不再混在总产品文档里。

它解决的是下面这几个问题：

1. 飞书这次到底做什么。
2. 哪些情况要发消息。
3. 消息里要带什么字段。
4. 需要什么配置。
5. 如何测试和验收。

一句话：

`飞书文档只定义通知功能，不定义整个矩阵系统。`


## 二、当前目标

本轮飞书只做最小可用版本，目标很明确：

1. 每次遇到需要你介入的问题时，发飞书消息。
2. 发布成功时，发飞书消息。

这里的重点不是做一个“完整消息平台”，而是先把最关键的两类信号稳定送出来。


## 三、本期范围

### 3.1 本期要做的

本期只做两类通知：

1. 问题通知
2. 发布成功通知

### 3.2 本期不做的

本期先不做下面这些：

1. `@人`
2. `@所有人`
3. 飞书日报
4. 失败汇总
5. 人工待办聚合
6. 复杂卡片编排
7. 飞书应用消息方案
8. 消息去重后台或通知中心页面

### 3.3 当前接入方式

本期统一走飞书群机器人 `Webhook` 方案。

原因：

1. 配置最少。
2. 落地最快。
3. 足够覆盖“异常通知 + 成功通知”。
4. 不需要引入更重的飞书应用鉴权链路。


## 四、通知类型

### 4.1 问题通知

问题通知不是“所有异常都发”，而是“需要你关注或介入的才发”。

当前建议纳入下面这些情况：

1. 任务进入 `manual_login_required`
2. 任务进入 `failed_terminal`
3. 账号进入 `session_expired`
4. 检测到 `account_identity_mismatch`
5. 自动核验后仍未确认成功，最终需要人工判断的 `publish_uncertain`

### 4.2 发布成功通知

发布成功通知只在任务最终进入 `published` 时发送一次。

不在下面这些时点提前发消息：

1. 只是点击了发布按钮
2. 只是页面看起来像跳转成功
3. 还在 `publish_verify` 阶段
4. 还没有最终链接或最终核验结果


## 五、触发规则

### 5.1 问题通知触发规则

只有当系统确认“这件事需要你看”时才发消息。

当前规则如下：

1. 自动重试中的普通中间失败，不发。
2. 同一条任务在短时间内重复进入同一中间失败，不发。
3. 真正进入人工登录阻塞、终态失败、账号身份异常、登录失效时，发。
4. 发布结果如果自动核验后仍不确定，发。

当前口径的核心是：

`飞书只负责发有效信号，不负责转发全部噪声。`

### 5.2 成功通知触发规则

成功通知的规则更简单：

1. 当 job 最终进入 `published` 时发送。
2. 同一条任务只发一次。
3. 成功消息优先带最终链接。


## 六、消息内容

### 6.1 问题通知最小字段

问题通知至少包含：

1. 矩阵账号名
2. 对应知乎账号名
3. 任务 ID
4. 当前阶段
5. 触发步骤
6. 失败类型
7. 失败原因
8. 问题标题
9. 问题链接或恢复入口

如果当前上下文里能拿到下面这些，也建议带上：

1. 当前账号绑定的知乎账号名
2. 最近一次计划发布时间
3. 恢复锚点阶段
4. 最后一次页面 URL

### 6.2 发布成功通知最小字段

发布成功通知至少包含：

1. 矩阵账号名
2. 对应知乎账号名
3. 任务 ID
4. 当前阶段
5. 发布步骤
6. 问题标题
7. 发布时间
8. 最终链接

如果当前上下文里能拿到，也建议带上：

1. 计划发布时间
2. 最终判定阶段
3. 对应账号的知乎账号名


## 七、消息模板建议

### 7.1 问题通知模板

建议先用简洁文本，不做复杂卡片。

示例：

```text
【知乎矩阵任务异常】
矩阵账号：焦花花
知乎账号：焦花花的知乎号
任务：#19
当前阶段：manual_login_required
触发步骤：login_checking
类型：session_expired
标题：量化新手刚开始要不要先学回测？
原因：当前账号登录态已失效，需要人工重新确认登录状态。
入口：https://www.zhihu.com/question/xxxx
```

### 7.2 发布成功通知模板

示例：

```text
【知乎发布成功】
矩阵账号：焦花花
知乎账号：焦花花的知乎号
任务：#14
当前阶段：published
发布步骤：publish_verify
标题：量化新手刚开始要不要先学回测？
时间：2026-03-27 14:26:00
链接：https://www.zhihu.com/question/xxxx/answer/xxxx
```


## 八、配置项

本期飞书功能只需要两个配置项：

1. `FEISHU_BOT_WEBHOOK_URL`
2. `FEISHU_BOT_SECRET`

说明如下：

### 8.1 `FEISHU_BOT_WEBHOOK_URL`

飞书群机器人的 `Webhook` 地址。

这是必填项。

### 8.2 `FEISHU_BOT_SECRET`

如果机器人开启了签名校验，则填写对应 `Secret`。

如果没有开启签名校验，这个字段可以为空。

### 8.3 配置原则

1. 真实 `Webhook` 和 `Secret` 不写进文档。
2. 真实配置只放环境变量。
3. 如果凭证疑似泄露，应及时重置机器人配置。


## 九、发送失败时的处理原则

飞书是辅助通知链路，不是主业务链路。

所以发送失败时必须遵守下面的原则：

1. 飞书发送失败不能阻塞主任务流。
2. 飞书发送失败不能影响数据库写入。
3. 飞书发送失败不能影响证据链落库。
4. 最多记录错误日志，必要时补记一条运行日志。

当前口径必须坚持：

`通知失败不等于任务失败。`


## 十、测试口径

飞书接入后，测试阶段至少验证下面几件事：

### 10.0 测试接口

后端提供测试接口：

`POST /notifications/feishu/test`

作用：

1. 不依赖真实发布任务，直接验证飞书机器人链路是否可用。
2. 不改数据库状态，不改任务状态。
3. 可用于确认 `Webhook` 和 `Secret` 是否配置正确。

### 10.1 问题通知验证

1. 人工登录阻塞时，能收到问题通知。
2. 终态失败时，能收到问题通知。
3. 账号身份不一致时，能收到问题通知。
4. 自动重试中的临时失败不会刷屏。
5. 消息里能明确区分矩阵账号、知乎账号、当前阶段和触发步骤。

### 10.2 成功通知验证

1. 任务最终进入 `published` 时，能收到成功通知。
2. 成功通知里带有最终链接。
3. 同一条任务不会重复发送多次成功通知。
4. 消息里能明确看出是哪个账号发布成功，以及最终发布收口发生在哪一步。

### 10.3 异常链路验证

1. `Webhook` 错误时，不影响任务继续执行。
2. `Secret` 错误时，不影响主任务写库。
3. 飞书不可达时，不影响 `publish_attempts`、`tool_traces`、`artifacts` 留存。


## 十一、当前验收标准

飞书这轮的验收只看最小功能是否成立。

验收标准如下：

1. 配置 `Webhook` 后，系统可以向飞书群正常发消息。
2. 任务进入 `manual_login_required` 时，系统会发送问题通知。
3. 任务进入 `failed_terminal` 时，系统会发送问题通知。
4. 任务最终进入 `published` 时，系统会发送成功通知。
5. 飞书发送失败不会影响主任务链路。


## 十二、后续扩展

等这轮最小通知稳定后，再考虑下一阶段扩展：

1. `@人`
2. `@所有人`
3. 日报
4. 失败汇总
5. 人工待办聚合
6. 飞书卡片模板
7. 飞书应用 API 方案
8. 通知去重、静默窗口和频控


## 十三、最终口径

截至当前，这份飞书文档对应的最终结论应该统一为：

1. 飞书已经纳入当前范围。
2. 当前只做最小通知，不做完整消息系统。
3. 只发两类消息：问题通知、发布成功通知。
4. 先用飞书群机器人 `Webhook` 跑通，再考虑更复杂的飞书能力。

一句话收尾：

`飞书这次不是做全，而是先做准。`
