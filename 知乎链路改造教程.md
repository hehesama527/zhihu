# 知乎链路改造教程

这份文档只讲项目里的知乎部分。朋友如果做的是完全不同的项目，不建议一上来全仓库重命名或全量改动，先把知乎链路里“业务定位、选题、写作、审核、发布、账号、前端展示”这几层改干净，再考虑包名和项目名。

## 1. 先看知乎链路的边界

当前项目是 monorepo，知乎链路主要分在这些位置：

| 位置 | 作用 | 改造优先级 |
| --- | --- | --- |
| `packages/core/src/config/env.ts` | 读取 `.env`、知乎基础地址、数据目录、关键词、模型、浏览器等运行配置 | 高 |
| `packages/core/src/prompts/default-prompts.ts` | Topic / Writer / Review / Publish / Note Agent 的默认提示词 | 最高 |
| `packages/core/src/services/zhihu-agent-context-service.ts` | 生成并读取知乎链路的全局业务目标 `target.md` 和产品资料 `product.md` | 最高 |
| `packages/core/src/services/account-prompt-context.ts` | 把账号人设、Soul、产品资料拼进各个 Agent Prompt | 高 |
| `packages/core/src/services/topic-discovery-service.ts` | 从知乎首页、通知、搜索关键词里采集候选问题 | 高 |
| `packages/core/src/services/topic-pipeline-service.ts` | 候选题 -> 题目卡 -> 草稿 -> 去 AI 味 -> 审核 -> 可发布内容 | 高 |
| `packages/core/src/services/review-service.ts` | 审核草稿的硬门槛、编辑质量、发布适配 | 中高 |
| `packages/core/src/services/publish-service.ts` | 用浏览器打开知乎问题页、找编辑器、粘贴、提交、校验结果 | 高，但最后改 |
| `packages/core/src/services/session-service.ts` | 登录状态检测、账号身份校验、人工登录恢复 | 中高 |
| `packages/core/src/repositories/*.ts` | 账号、任务、选题、排期、发布记录的 MySQL 读写 | 中 |
| `packages/shared/src/types.ts` 和 `packages/shared/src/schemas.ts` | 前后端共享类型、接口入参校验、状态枚举 | 中 |
| `apps/api/src/server.ts` | 知乎 API 服务入口，暴露账号、任务、选题、排期、Prompt、恢复接口 | 中 |
| `apps/worker/src/index.ts` | 知乎后台 Worker 入口，定时推进任务 | 中 |
| `apps/web/src/app/(zhihu)/zhihu/*` | 知乎前端页面 | 低到中 |
| `apps/web/src/components/*` | 知乎账号、任务、Prompt、Worker 等前端组件 | 低到中 |

不要从这些目录开始改：

| 位置 | 原因 |
| --- | --- |
| `dist/` | 编译产物，源码改完后重新 build 生成 |
| `.next/` | Next.js 构建缓存 |
| `node_modules/` | 依赖目录 |
| `data/profiles/` | 浏览器真实登录 Profile，误删会导致账号重新登录 |
| `data/screenshots/`、`data/logs/`、`.runlogs/` | 运行证据和日志，不是业务逻辑 |
| `apps/x-*`、`packages/x-*` | X/Twitter 链路，和这份教程无关 |

## 2. 当前知乎链路怎么跑

理解这条链很重要，不然容易只改 Prompt，却发现发布、审核或选题还在旧业务上。

主流程是：

1. `apps/api/src/server.ts` 启动 API，初始化数据库、默认 Prompt、默认账号、当天排期。
2. `apps/worker/src/index.ts` 启动 Worker，每隔 `WORKER_INTERVAL_MS` 执行一次 `WorkerRunner.tick()`。
3. `WorkerRunner` 给每个账号补当天排期和待发布任务。
4. 如果没有临近待发布任务，`TopicDiscoveryService` 会用账号独立浏览器 Profile 登录知乎，采集候选问题。
5. `TopicDiscoveryService` 通过 Topic Agent 预筛候选题，写入 `topic_candidates`。
6. `TopicPipelineService` 从候选池选题，生成 `topic_cards`。
7. Writer Agent 生成草稿，写入 `drafts`。
8. `HumanizerService` 做去 AI 味处理，继续写入 `drafts`。
9. `ReviewService` 调用 Review Agent 做硬门槛、编辑质量、发布适配审核，写入 `reviews`。
10. 审核通过后，`publish_jobs` 变成 `review_passed`。
11. 到计划时间后，`PublishService` 用浏览器进入知乎问题页，粘贴内容并提交。
12. 发布成功后，任务写入 `published`，记录最终链接，并写入 `answered_topics` 做重复题拦截。

核心状态在 `packages/shared/src/types.ts` 的 `JobStatus`：

```ts
queued
topic_discovery
topic_agent
topic_review
writer
humanizing
review_hard_gate
review_editorial
review_publish
review_passed
login_checking
publishing
publish_verify
retry_waiting
manual_login_required
needs_manual_review
published
failed_terminal
```

如果朋友只是换一个业务项目，通常不需要改状态枚举。除非他要把“知乎回答”改成完全不同的发布对象，比如文章、视频、评论或别的平台，那才需要改类型和数据表。

## 3. 第一批必须改：业务目标和产品资料

这一步是最重要的。当前项目里的知乎内容围绕加密交易工具 `CryptoPathX`，如果朋友做的是不同项目，必须先换掉这层，否则 Agent 会持续围绕旧产品选题和写作。

### 3.1 改全局业务资料

文件：

```text
packages/core/src/services/zhihu-agent-context-service.ts
```

这里定义了默认的两个文档：

```ts
DEFAULT_TARGET_MARKDOWN
DEFAULT_PRODUCT_MARKDOWN
```

运行时实际会生成或读取：

```text
data/zhihu-agent-context/target.md
data/zhihu-agent-context/product.md
```

也可以用环境变量改路径：

```env
ZHIHU_AGENT_CONTEXT_DIR=H:\your-project\data\zhihu-agent-context
ZHIHU_TARGET_CONTEXT_PATH=H:\your-project\data\zhihu-agent-context\target.md
ZHIHU_PRODUCT_CONTEXT_PATH=H:\your-project\data\zhihu-agent-context\product.md
```

建议朋友不要只改 TypeScript 里的默认值，也要检查 `data/zhihu-agent-context/target.md` 和 `product.md` 是否已经存在。因为一旦文件存在，系统会优先读取现有文件，不会再覆盖成新的默认内容。

`target.md` 应该写清楚：

1. 这条知乎链路为什么发内容。
2. 目标读者是谁。
3. 希望读者形成什么认知或行动。
4. 哪些题适合回答，哪些题不适合。
5. 什么时候可以自然提产品，什么时候必须纯分享。
6. 成功内容的判断标准。

`product.md` 应该写清楚：

1. 产品一句话定位。
2. 目标用户。
3. 核心功能。
4. 真实优势。
5. 禁止夸大的能力。
6. 竞品或替代方案边界。
7. 在知乎回答里允许出现的表达方式。
8. 不允许出现的营销话术。

如果朋友的项目不是产品推广，而是个人 IP、课程、服务、招聘、开源项目，也一样要写成“目标 + 可说边界 + 不可说边界”，不要只写口号。

### 3.2 如果只想改运行资料，不改源码

可以直接准备这两个文件：

```text
data/zhihu-agent-context/target.md
data/zhihu-agent-context/product.md
```

然后重启 API 和 Worker。这样不需要动 `zhihu-agent-context-service.ts`。

但如果要把项目交给别人长期维护，建议同时改源码里的默认值，避免新环境初始化时又生成旧业务资料。

## 4. 第二批必须改：Agent 默认 Prompt

文件：

```text
packages/core/src/prompts/default-prompts.ts
```

这里是知乎链路最核心的默认 Prompt 种子。当前包括：

| Prompt 名 | 作用 |
| --- | --- |
| `topic_agent` | 判断一个知乎问题要不要写、优先级、切入角度、是否软广、写作计划 |
| `writer_agent` | 生成知乎回答草稿 |
| `review_agent` | 审核草稿是否能发，是否跑偏、硬广、重复、风险过高 |
| `publish_agent` | 根据浏览器页面证据决定发布动作和发布结果 |
| `zhihu_note_agent` | 手动学习外部账号表达方式，产出账号 Soul 候选 |

朋友换项目时，至少要改这三个：

```text
topic_agent
writer_agent
review_agent
```

`publish_agent` 只要还在知乎发回答，通常不用大改。`zhihu_note_agent` 如果还要做账号风格学习，就保留；如果不需要，可以暂时不用。

### 4.1 Topic Agent 要改什么

重点改这些：

1. 选题领域：比如从加密交易改成教育、SaaS、法律咨询、家装、B2B 服务等。
2. 有效题标准：什么问题值得写，什么问题必须跳过。
3. 读者痛点：当前项目关心的是交易风险和策略验证，朋友项目要换成自己的读者痛点。
4. 产品承接条件：什么时候允许出现产品，什么时候禁止硬塞。
5. 输出 JSON 字段可以先不改，除非业务确实不需要 `soft_promo_directive`、`writing_plan` 这些字段。

建议保留这个输出结构：

```json
{
  "title": "",
  "summary": "",
  "priority": "P0 | P1 | P2 | SKIP",
  "fit_score": 0,
  "question_type": "",
  "persona_mode": "",
  "target_audience": [],
  "pain_points": [],
  "recommended_angle": "",
  "soft_promo_mode": "none | light | natural",
  "should_include_soft_promo": false,
  "soft_promo_directive": {},
  "writing_plan": {},
  "must_avoid": [],
  "risk_notes": [],
  "topic_fingerprint": {}
}
```

因为后面的 `TopicPipelineService`、`ReviewService` 和前端任务详情页都会读这些字段。

### 4.2 Writer Agent 要改什么

当前 Writer Prompt 里有大量旧项目的人设、行业、产品名、风险边界。朋友必须全量替换：

1. 默认叙述者是谁。
2. 读者是谁。
3. 文章像什么，不像什么。
4. 是否用第一人称。
5. 允许使用哪些案例。
6. 是否允许提产品。
7. 产品最多出现几次。
8. 禁止承诺什么。
9. 输出 JSON 格式。

不要只搜索替换产品名。旧项目里有很多隐含行业规则，比如“币圈”“交易”“回测”“K 线”“合约”“杠杆”“CryptoPathX”。如果朋友项目完全不同，这些都要删掉或重写。

建议保留 Writer 输出格式：

```json
{
  "title": "建议标题",
  "summary": "摘要",
  "content": "完整回答正文",
  "fingerprint": {
    "opening_angle": "",
    "core_claims": [],
    "case_structure": "",
    "closing_style": ""
  }
}
```

`TopicPipelineService` 主要读取 `content`、`summary` 和 `title`。如果改输出格式，必须同步改 `packages/core/src/services/topic-pipeline-service.ts`。

### 4.3 Review Agent 要改什么

Review Agent 负责决定能不能发。朋友要把这些规则换成新业务规则：

1. 硬性禁区：法律、医疗、金融、教育承诺、效果保证、隐私、夸大宣传等，按新项目实际写。
2. 软广质量：产品出现是否自然，是否解决当前问题，是否硬广。
3. 内容质量：是否像知乎回答，是否空泛，是否重复，是否有案例或证据。
4. 发布风险：是否容易被平台判定营销、诱导、违规。

建议保留输出结构：

```json
{
  "hardGate": {
    "decision": "PASS | BLOCK",
    "issues": [],
    "reason": ""
  },
  "editorial": {
    "decision": "PASS | REVISE",
    "issues": [],
    "score": 0,
    "strengths": [],
    "rewrite_brief": "",
    "quality": {}
  },
  "publish": {
    "decision": "PASS | REVISE | BLOCK_DUPLICATION",
    "issues": [],
    "publish_ready": true,
    "duplicate_reason": "",
    "matched_past_contents": [],
    "review_summary": "",
    "approved_content": ""
  }
}
```

`ReviewService` 会依赖这些字段判断是通过、改写、阻断还是人工复核。

## 5. 第三批必须改：选题来源和关键词

文件：

```text
packages/core/src/services/topic-discovery-service.ts
packages/core/src/config/env.ts
.env
.env.example
```

当前选题来源有三类：

1. 知乎首页推荐：`recommended_answer`
2. 通知/邀请回答：`invite_answer`
3. 搜索关键词：`keyword_search`

在 `TopicDiscoveryService.harvestCandidates()` 里可以看到：

```ts
url: `${getAppConfig().zhihuBaseUrl}/`
url: `${getAppConfig().zhihuBaseUrl}/notifications`
url: `${getAppConfig().zhihuBaseUrl}/search?type=content&q=${encodeURIComponent(keyword)}`
```

关键词来自：

```env
TOPIC_KEYWORDS=关键词1,关键词2,关键词3
```

朋友需要改：

1. `.env` 里的 `TOPIC_KEYWORDS`。
2. `.env.example` 里的示例关键词。
3. `packages/core/src/config/env.ts` 里的默认关键词。
4. `topic_agent` 里的有效题判断规则。
5. 如果新项目不适合首页推荐或邀请回答，可以在 `topic-discovery-service.ts` 暂时关闭对应来源，只保留关键词搜索。

如果朋友刚开始跑新项目，建议先只用关键词搜索，不要一开始就放开首页推荐和通知。原因是关键词来源更可控，容易判断 Topic Agent 是否筛得准。

可以这样做：

1. 保留 `keyword_search`。
2. 暂时注释或跳过首页推荐和通知采集。
3. 让 `TOPIC_KEYWORDS` 覆盖 10 到 30 个高意图关键词。
4. 先观察 `topic_candidates` 里的题是否符合业务。

## 6. 第四批必须改：账号人设和 Soul

相关文件：

```text
packages/core/src/repositories/account-repository.ts
packages/core/src/services/account-soul-service.ts
packages/core/src/services/account-prompt-context.ts
apps/web/src/components/account-panel.tsx
apps/web/src/components/account-soul-panel.tsx
```

默认账号在：

```text
packages/core/src/repositories/account-repository.ts
```

当前有：

```ts
DEFAULT_ACCOUNT_NAME
DEFAULT_ZHIHU_USER_NAME
DEFAULT_RISK_DOMAIN
```

朋友要改成自己的默认账号名和知乎用户名，或者直接在前端“账号管理”里新增账号。

账号 Soul 是更重要的长期资料。它会影响：

1. Topic Agent 选题是否适合这个账号。
2. Writer Agent 写出来像不像这个账号。
3. Review Agent 是否判定人设跑偏。
4. 每个任务会保存 Soul 快照，避免任务创建后账号资料变化污染旧任务。

运行时 Soul 文件通常在：

```text
data/account-souls/
```

具体文件由 `AccountSoulService` 管理。

建议朋友给每个账号至少写这些：

1. 核心身份：这个账号是谁。
2. 目标读者：写给谁看。
3. 语气：克制、犀利、温和、经验型、顾问型等。
4. 世界观：这个账号相信什么，不相信什么。
5. 证据锚点：常用什么案例、经验、数据、观察方式。
6. 产品提及策略：何时可提，何时不能提。
7. 硬边界：绝对不说什么。
8. 禁用词：哪些表达会显得假、硬广、违规或不符合人设。

不要把 Soul 写成一篇广告。Soul 是账号稳定身份，不是单篇文章脚本。

## 7. 第五批按需改：写作流水线

文件：

```text
packages/core/src/services/topic-pipeline-service.ts
packages/core/src/services/humanizer-service.ts
packages/core/src/services/review-service.ts
packages/core/src/services/zhihu-case-research-service.ts
```

### 7.1 `topic-pipeline-service.ts`

这份文件决定了从选题到可发布内容的流程：

```text
候选题
-> Topic Agent 题目卡
-> case research
-> Writer Agent 草稿
-> Humanizer 去 AI 味
-> Review Agent 审核
-> review_passed
```

朋友如果只是换业务，不建议先改流程。优先改 Prompt 和上下文资料。

只有下面情况才改它：

1. 新项目不需要 `case_research`。
2. 新项目不需要 `HumanizerService`。
3. 新项目要多轮事实核查。
4. 新项目要接入外部资料库。
5. 新项目的 Writer 输出字段变了。
6. 新项目审核通过条件变了。

### 7.2 `zhihu-case-research-service.ts`

当前项目里这个服务偏加密市场资料，会收集新闻、行情、CoinGecko/Binance 等材料。朋友项目完全不同的话，这个文件要重点处理。

选择有三个：

1. 先关掉：让 `attachCaseResearchToTopicCard()` 不再补充 case research。
2. 改成新项目资料源：比如行业新闻、官网文档、知识库、案例库。
3. 保留接口形状，但返回空结果：先跑通主链路，再慢慢补资料。

如果新业务没有外部事实资料需求，建议先走第 3 种，最稳。

### 7.3 `humanizer-service.ts`

当前去 AI 味服务依赖：

```env
CODEX_HOME=
HUMANIZER_SKILL_PATH=
```

如果朋友机器上没有这个 skill，草稿阶段可能失败。可以选择：

1. 安装同样的 humanizer skill。
2. 修改 `HumanizerService.humanize()`，让它直接返回原文。
3. 改成自己的润色模型或本地脚本。

刚迁移时建议先让它直接返回原文，等主链路跑通后再加去 AI 味。

## 8. 第六批最后改：知乎发布自动化

文件：

```text
packages/core/src/services/publish-service.ts
packages/core/src/services/browser-skill-service.ts
packages/core/src/services/session-service.ts
packages/core/src/utils/zhihu-url.ts
packages/core/src/utils/stealth-inject.ts
packages/core/src/utils/browser.ts
```

只要朋友仍然是在知乎“回答问题”，这里尽量少改。

`publish-service.ts` 里最敏感的是：

```ts
EDITOR_SELECTORS
SUBMIT_TEXT_CANDIDATES
DIRECT_SUBMIT_SELECTORS
```

这些负责找知乎编辑器和提交按钮。如果知乎页面变化，才需要改。

如果朋友不是发回答，而是发文章、想法、专栏或别的平台内容，那这里就不是小改了，需要重写发布动作：

1. 目标 URL 怎么生成。
2. 页面打开后如何判断登录状态。
3. 如何找到编辑器。
4. 如何处理富文本、加粗、图片。
5. 如何点击提交。
6. 如何判断发布成功。
7. 如何记录最终链接。

`session-service.ts` 负责登录状态和身份检测。换成新账号、新项目时，一般只需要确保：

```env
ZHIHU_BASE_URL=https://www.zhihu.com
BROWSER_CHANNEL=msedge
```

以及账号的 `profile_dir` 可用。

如果朋友不需要自动发布，只需要选题和草稿，可以先在 Worker 里停在 `review_passed`，不要让任务进入 `publishing`。

## 9. 第七批改：数据库字段和初始化脚本

文件：

```text
packages/core/src/db/sql.ts
apps/api/src/scripts/init-db.ts
zhihu_mvp.sql
```

当前核心表：

| 表 | 作用 |
| --- | --- |
| `accounts` | 知乎账号、用户名、风险域、状态、浏览器 Profile |
| `topic_candidates` | 采集到的知乎问题候选池 |
| `topic_cards` | Topic Agent 产出的题目卡 |
| `drafts` | 原始草稿和去 AI 味草稿 |
| `reviews` | 审核结果和最终可发布内容 |
| `publish_jobs` | 发布任务主表 |
| `answered_topics` | 已回答问题去重库 |
| `publish_attempts` | 每次发布尝试 |
| `artifacts` | 截图等产物 |
| `tool_traces` | 浏览器工具轨迹 |
| `skill_runs` | skill/agent 运行记录 |
| `daily_publish_schedule` | 每日排期 |
| `prompt_sets`、`prompt_versions`、`prompt_activations` | Prompt 管理 |
| `ops_incidents` | 运维诊断问题 |
| `zhihu_scraped_content` | 知乎抓取内容缓存 |

朋友只是换业务领域，一般不需要改表结构。需要重点改的是数据库名：

```env
MYSQL_URL=mysql://root:your_password@127.0.0.1:6306/your_project_db
```

如果要改表字段，注意同步这些文件：

1. `packages/core/src/db/sql.ts`
2. 对应 repository，例如 `job-repository.ts`、`topic-repository.ts`
3. `packages/shared/src/types.ts`
4. `packages/shared/src/schemas.ts`
5. 前端页面和组件

不要直接改 `zhihu_mvp.sql` 后就以为系统生效。运行时迁移逻辑主要在 `packages/core/src/db/sql.ts`。

## 10. 第八批改：API 和前端文案

API 文件：

```text
apps/api/src/server.ts
apps/web/src/lib/api.ts
```

前端页面：

```text
apps/web/src/app/(zhihu)/zhihu/page.tsx
apps/web/src/app/(zhihu)/zhihu/account/page.tsx
apps/web/src/app/(zhihu)/zhihu/topics/page.tsx
apps/web/src/app/(zhihu)/zhihu/publish-jobs/page.tsx
apps/web/src/app/(zhihu)/zhihu/jobs/[id]/page.tsx
apps/web/src/app/(zhihu)/zhihu/schedule/page.tsx
apps/web/src/app/(zhihu)/zhihu/prompts/page.tsx
apps/web/src/app/(zhihu)/zhihu/ops/page.tsx
apps/web/src/app/models/zhihu/page.tsx
```

组件：

```text
apps/web/src/components/account-panel.tsx
apps/web/src/components/account-registry.tsx
apps/web/src/components/account-soul-panel.tsx
apps/web/src/components/worker-panel.tsx
apps/web/src/components/prompt-studio.tsx
apps/web/src/components/zhihu-note-agent-panel.tsx
apps/web/src/components/status-chip.tsx
```

前端不是第一优先级。先保证后端和 Worker 能产出正确任务，再改页面文案。

必须改的前端内容：

1. 页面标题和描述里旧项目业务名。
2. “知乎账号”“矩阵”等术语是否仍然适合。
3. Prompt 管理页里的说明。
4. 任务详情页里展示的字段名。
5. 首页入口 `apps/web/src/app/page.tsx` 里 `/zhihu` 卡片文案。

如果朋友只是内部用，前端可以晚一点改。只要 `apps/web/src/lib/api.ts` 的 API base URL 正确，功能能跑。

## 11. 环境变量要改哪些

最小必改：

```env
MYSQL_URL=mysql://root:your_password@127.0.0.1:6306/your_project_db
TOPIC_KEYWORDS=新项目关键词1,新项目关键词2,新项目关键词3
ZHIHU_AGENT_MODEL=你的模型
ZHIHU_AGENT_BASE_URL=你的模型服务地址
ZHIHU_AGENT_API_KEY=你的 key
ZHIHU_AGENT_WIRE_API=chat_completions
ZHIHU_AGENT_REASONING_EFFORT=high
APP_TIMEZONE=Asia/Shanghai
WORKER_INTERVAL_MS=45000
ZHIHU_BASE_URL=https://www.zhihu.com
BROWSER_CHANNEL=msedge
```

如果用前端：

```env
INTERNAL_API_BASE_URL=http://127.0.0.1:8787
NEXT_PUBLIC_API_BASE_URL=/api
WEB_URL=http://localhost:3000
API_URL=http://localhost:8787
API_PORT=8787
```

如果用飞书通知：

```env
FEISHU_BOT_WEBHOOK_URL=
FEISHU_BOT_SECRET=
```

如果保留去 AI 味：

```env
CODEX_HOME=C:\Users\your_user\.codex
HUMANIZER_SKILL_PATH=C:\Users\your_user\.codex\skills\humanizer-zh\SKILL.md
```

如果想把全局业务资料放到指定目录：

```env
ZHIHU_AGENT_CONTEXT_DIR=H:\your-project\data\zhihu-agent-context
ZHIHU_TARGET_CONTEXT_PATH=H:\your-project\data\zhihu-agent-context\target.md
ZHIHU_PRODUCT_CONTEXT_PATH=H:\your-project\data\zhihu-agent-context\product.md
```

## 12. 推荐改造顺序

按这个顺序最稳：

1. 新建自己的数据库，改 `MYSQL_URL`。
2. 改 `.env` 和 `.env.example` 里的模型、关键词、端口和路径。
3. 改 `data/zhihu-agent-context/target.md` 和 `product.md`，或者改 `zhihu-agent-context-service.ts` 默认内容。
4. 改 `default-prompts.ts` 的 `topic_agent`、`writer_agent`、`review_agent`。
5. 改 `account-repository.ts` 的默认账号名，或者启动后在前端新增账号。
6. 在前端账号页维护每个账号的 Soul。
7. 先只跑选题采集，检查 `topic_candidates` 是否符合业务。
8. 再跑草稿生成，检查 `drafts` 和 `reviews`。
9. 草稿稳定后再开启浏览器发布。
10. 最后改前端页面文案、项目名、包名和部署脚本。

不要反过来先做 UI 改名。UI 改得再干净，只要 Prompt 和 target/product 还是旧项目，产出的内容还是旧项目。

## 13. 最小验证流程

### 13.1 安装和初始化

```powershell
npm install
npm run db:init
npm run typecheck
```

如果只改文档和 Prompt，`typecheck` 不一定能发现 Prompt 语义问题，但至少能确认 TypeScript 没坏。

### 13.2 启动知乎链路

只跑知乎主链路时，用：

```powershell
npm run dev
```

这个命令会同时启动：

```text
@zhihu-mvp/api
@zhihu-mvp/control-api
@zhihu-mvp/hotspot-api
@zhihu-mvp/worker
@zhihu-mvp/ops-agent
@zhihu-mvp/web
```

如果朋友只想先看前端：

```powershell
npm run dev:frontend
```

如果只想跑后端和 Worker：

```powershell
npm run dev:backend
```

### 13.3 先人工登录账号

进入前端：

```text
http://localhost:3000/zhihu/account
```

流程：

1. 新建或选择账号。
2. 点人工登录。
3. 在打开的浏览器里登录知乎。
4. 回到前端确认登录恢复。
5. 确认账号状态从 `manual_login_required` 变成可运行。

### 13.4 先验证选题

进入：

```text
http://localhost:3000/zhihu/topics
```

看三件事：

1. 采集到的问题是不是新项目领域。
2. `priority` 和 `fit_score` 是否合理。
3. `validity_reason` 是否说明清楚。

如果这里已经跑偏，先改 `TOPIC_KEYWORDS`、`target.md`、`product.md` 和 `topic_agent`，不要急着看草稿。

### 13.5 再验证草稿和审核

进入：

```text
http://localhost:3000/zhihu/publish-jobs
http://localhost:3000/zhihu/jobs/{id}
```

检查：

1. `topicOutputJson` 是否符合新业务。
2. 原始草稿是否像目标账号。
3. 去 AI 味后正文是否可读。
4. 审核 JSON 是否能指出真实问题。
5. `approvedContent` 是否是最终可发布版本。

如果草稿还是旧项目口吻，优先改：

```text
default-prompts.ts -> writer_agent
account-prompt-context.ts
账号 Soul
target.md
product.md
```

### 13.6 最后验证发布

只有前面都稳定后，再让任务进入发布。

观察：

1. `publish_attempts` 是否有记录。
2. `tool_traces` 是否能看到打开页面、点击、粘贴、提交。
3. `artifacts` 是否有截图。
4. `final_url` 是否写回。
5. `answered_topics` 是否写入去重记录。

如果发布失败，优先看 `jobs/{id}` 详情页里的：

1. 失败分类。
2. 当前阶段。
3. 恢复锚点。
4. 浏览器截图。
5. 工具轨迹。

## 14. 常见坑

### 14.1 只改产品名，不改选题标准

这是最常见的问题。Writer 可能会提新产品名，但 Topic Agent 仍然选旧领域问题，内容会非常别扭。

必须同时改：

```text
TOPIC_KEYWORDS
target.md
product.md
topic_agent
writer_agent
review_agent
```

### 14.2 `data/zhihu-agent-context` 旧文件没删

如果 `data/zhihu-agent-context/target.md` 和 `product.md` 已经存在，系统会读现有文件。你改了 `zhihu-agent-context-service.ts` 的默认值，也不会自动覆盖旧文件。

解决方式：

1. 直接编辑 `data/zhihu-agent-context/target.md` 和 `product.md`。
2. 或者换 `ZHIHU_AGENT_CONTEXT_DIR` 到新目录。
3. 或者确认无用后删除旧文件，让系统重新生成。

### 14.3 忘了账号 Soul

全局 Prompt 改对了，但账号 Soul 还是旧身份，Writer 还是会往旧口吻靠。

解决方式：

1. 前端账号页更新 Soul。
2. 新建账号时写清楚 `name` 和 `zhihuUserName`。
3. 老任务已经保存了 Soul 快照，必要时新建任务重跑。

### 14.4 直接改 `dist`

不要改：

```text
apps/api/dist
apps/worker/dist
packages/core/dist
packages/shared/dist
apps/web/.next
```

这些都是构建产物。改源码后运行：

```powershell
npm run build
```

### 14.5 Humanizer 环境没有准备

如果 `HUMANIZER_SKILL_PATH` 不存在，草稿可能卡在 `humanizing`。

迁移早期可以先让 `HumanizerService` 直接返回原文，等主链路跑通后再恢复。

### 14.6 还没登录就跑 Worker

账号没登录时，Worker 会把账号和任务打到 `manual_login_required`。这是正常保护，不是系统坏了。

先去：

```text
/zhihu/account
```

完成人工登录和恢复确认。

### 14.7 新业务不适合自动发布

有些项目内容风险更高，比如法律、医疗、金融建议、教育承诺、招聘、投资咨询。这类项目建议先只跑到 `review_passed`，人工看完再发，不要一开始全自动发布。

## 15. 哪些地方可以暂时不改

如果朋友只是想快速复用知乎内容链路，可以暂时不改：

1. 数据表名。
2. `publish_jobs` 状态流。
3. 前端路由 `/zhihu`。
4. 包名 `@zhihu-mvp/*`。
5. `PublishService` 的浏览器发布逻辑。
6. `PromptService` 的版本管理逻辑。
7. `ops` 运维诊断逻辑。

先把内容正确性跑通，比项目名彻底清理更重要。

## 16. 如果要彻底改名

彻底改名不是第一步。如果后面确实要把项目从 `zhihu-mvp` 改成新项目名，需要同步这些位置：

1. 根目录 `package.json` 的 `name` 和 scripts。
2. `apps/*/package.json` 里的包名。
3. `packages/*/package.json` 里的包名。
4. 所有 import 里的 `@zhihu-mvp/core`、`@zhihu-mvp/shared`。
5. `package-lock.json`。
6. `.env` 里的数据库名、目录名。
7. 前端页面文案。

建议等业务链路已经能稳定产出新项目内容后，再做这一步。

## 17. 给朋友的最短执行清单

如果只给他一张清单，就按这个来：

1. 改 `.env`：数据库、模型、关键词、端口。
2. 改 `data/zhihu-agent-context/target.md`。
3. 改 `data/zhihu-agent-context/product.md`。
4. 改 `packages/core/src/prompts/default-prompts.ts` 里的 `topic_agent`。
5. 改 `packages/core/src/prompts/default-prompts.ts` 里的 `writer_agent`。
6. 改 `packages/core/src/prompts/default-prompts.ts` 里的 `review_agent`。
7. 改或新增账号，并维护账号 Soul。
8. 跑 `npm run db:init`。
9. 跑 `npm run typecheck`。
10. 启动服务，先登录账号。
11. 看 `/zhihu/topics`，确认选题对。
12. 看 `/zhihu/jobs/{id}`，确认草稿和审核对。
13. 最后才测试自动发布。

一句话总结：

> 这个知乎部分真正要迁移的不是“页面文案”，而是 `target/product`、`Topic Agent`、`Writer Agent`、`Review Agent`、关键词和账号 Soul。只要这六块不改干净，项目表面换了名字，实际产出的内容仍然会是旧项目。
