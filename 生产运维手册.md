# 知乎矩阵自动化发布系统生产运维手册

## 1. 项目概览

这是一个单机多进程 Node.js 应用，目标是把知乎矩阵账号的选题、写作、审核、发布、通知串成一条自动化链路。

运行时一共 3 个核心进程：

| 服务 | 目录 | 作用 | 默认端口 |
| --- | --- | --- | --- |
| API | `apps/api` | 提供后台接口、手动登录恢复、数据查询、手动触发 worker | `8787` |
| Worker | `apps/worker` | 常驻轮询数据库，推进选题、写作、审核、发布、重试 | 无 HTTP 端口 |
| Web | `apps/web` | Next.js 管理台，供运营/运维查看账号、任务、证据链 | `3000` |

关键事实：

- API 有健康检查接口：`GET /health`
- Worker 没有独立健康接口，它是常驻定时轮询进程，轮询间隔来自 `WORKER_INTERVAL_MS`
- 数据库 schema 会在 API/Worker 启动时自动迁移；首次部署建议仍然显式执行一次 `db:init`
- 当前 API 没有鉴权，且 CORS 是全开放的；生产环境必须放在内网、VPN、堡垒机或反向代理鉴权后面

## 2. 代码和数据目录

### 2.1 代码目录

- `apps/api`
- `apps/worker`
- `apps/web`
- `packages/core`
- `packages/shared`

### 2.2 运行时目录

默认运行目录来自 `DATA_DIR`。如果不配，默认就是仓库根目录下的 `data`。

常用目录：

- `data/profiles/account-N`
  账号浏览器 Profile 根目录。实际 Playwright/Edge 会继续落到 `data/profiles/account-N/msedge`
- `data/locks`
  人工登录锁文件目录，例如 `manual-login-account-1.lock`
- `data/screenshots`
  运行期截图证据
- `output/playwright`
  Playwright 相关输出
- `output/*.prompt.txt`
  Prompt 快照，便于回溯模型输入

补充说明：

- 仓库里还有一个 `data-edge` 目录，这是历史数据目录，不是当前默认运行目录
- 当前代码默认吃的是 `data`，不要把 `data` 和 `data-edge` 混用

## 3. 运行依赖

本机当前验证环境：

- Node.js `v24.13.0`
- npm `11.6.2`
- MySQL 连接由 `MYSQL_URL` 提供
- 浏览器通道默认使用 `msedge`

生产最低要求建议：

- Windows 主机
- 安装 Microsoft Edge
- 本机可访问 MySQL
- 本机可访问知乎
- 本机可访问所选 LLM 提供商

## 4. 环境变量

### 4.1 必配项

- `MYSQL_URL`
- `API_PORT`
- `WEB_URL`
- `API_URL`
- `NEXT_PUBLIC_API_BASE_URL`
- `APP_TIMEZONE`
- `BROWSER_CHANNEL`
- `WORKER_INTERVAL_MS`
- `TOPIC_KEYWORDS`
- `ZHIHU_AGENT_MODEL`
- `ZHIHU_AGENT_BASE_URL`
- `ZHIHU_AGENT_API_KEY`
- `ZHIHU_AGENT_WIRE_API`
- `ZHIHU_AGENT_REASONING_EFFORT`

### 4.2 推荐配置

- `FEISHU_BOT_WEBHOOK_URL`
- `FEISHU_BOT_SECRET`

### 4.3 可选项

- `DATA_DIR`
- `HTTP_PROXY`
- `HTTPS_PROXY`
- `ALL_PROXY`
- `CODEX_HOME`
- `CODEX_CONFIG_PATH`
- `CODEX_AUTH_PATH`

说明：

- `NEXT_PUBLIC_API_BASE_URL` 是前端公开变量，生产地址变更后要先改环境变量，再重新 `build`
- `HTTP_PROXY` / `HTTPS_PROXY` 会被 LLM HTTP 客户端读取。如果代理不稳定，会影响模型调用
- 浏览器自动化是否走代理，更多取决于浏览器自身配置和 Profile 状态，不要把“LLM 代理正常”误判成“知乎页面访问也正常”

## 5. 初始化、构建、启动

### 5.1 首次部署

在仓库根目录执行：

```powershell
npm ci
npm run build
npm run db:init -w @zhihu-mvp/api
```

说明：

- `db:init` 会创建数据库、建表、跑迁移、初始化默认 prompt、确保默认账号存在
- 如果只是代码升级，通常不需要删库，重新跑 `db:init` 即可

### 5.2 生产启动命令

不要在生产环境使用下面这些开发命令：

- `npm run dev`
- `npm run dev:backend`
- `npm run dev:frontend`

生产环境应启动每个 workspace 的 `start`：

```powershell
npm run start -w @zhihu-mvp/api
npm run start -w @zhihu-mvp/worker
npm run start -w @zhihu-mvp/web -- --port 3000
```

建议用 PM2 或 NSSM 之类的进程守护工具托管。PM2 示例：

```powershell
pm2 start "npm run start -w @zhihu-mvp/api" --name zhihu-api
pm2 start "npm run start -w @zhihu-mvp/worker" --name zhihu-worker
pm2 start "npm run start -w @zhihu-mvp/web -- --port 3000" --name zhihu-web
pm2 save
```

### 5.3 推荐启动顺序

1. MySQL
2. `npm run db:init -w @zhihu-mvp/api`
3. API
4. Worker
5. Web

## 6. 停服、重启、巡检

### 6.1 停服

如果使用 PM2：

```powershell
pm2 stop zhihu-web
pm2 stop zhihu-worker
pm2 stop zhihu-api
```

如果是直接命令行启动：

```powershell
Get-CimInstance Win32_Process |
  Where-Object {
    $_.Name -eq 'node.exe' -and (
      $_.CommandLine -like '*H:\claw*' -or
      $_.CommandLine -like '*@zhihu-mvp*'
    )
  } |
  Select-Object ProcessId, CommandLine
```

确认后再停：

```powershell
Stop-Process -Id <PID列表> -Force
```

### 6.2 重启

顺序建议：

1. 先停 Worker
2. 再停 API
3. 再停 Web
4. 升级代码 / 更新 `.env`
5. `npm run build`
6. 启动 API
7. 启动 Worker
8. 启动 Web

### 6.3 基本巡检

API 存活：

```powershell
curl http://127.0.0.1:8787/health
```

飞书链路：

```powershell
curl -X POST http://127.0.0.1:8787/notifications/feishu/test
```

手动触发一轮 worker：

```powershell
curl -X POST http://127.0.0.1:8787/worker/tick
```

查看账号状态：

```powershell
curl http://127.0.0.1:8787/accounts
```

查看任务：

```powershell
curl http://127.0.0.1:8787/jobs
```

## 7. 日常运维关注点

### 7.1 账号浏览器 Profile

账号 Profile 默认路径：

- `data/profiles/account-1/msedge`
- `data/profiles/account-2/msedge`
- 以此类推

只要知乎登录态、账号身份、浏览器缓存出了问题，最先受影响的通常不是 API，而是发布链路。

优先处理顺序：

1. 先确认账号是不是 `manual_login_required` / `session_expired`
2. 通过 Web 或接口发起人工登录恢复
3. 如果恢复后仍然报浏览器/网络异常，备份后重置该账号的 Profile

### 7.2 证据链

排障时优先看这几类证据：

- `publish_attempts`
- `tool_traces`
- `artifacts`
- `data/screenshots`
- `output/playwright`

不要只看 `publish_jobs.status`，因为真正出错点往往藏在 `tool_traces` 和截图里。

### 7.3 日志

当前应用主要把日志打到标准输出。生产上一定要让 PM2、Windows 服务管理器或日志采集器接住 stdout/stderr。

仓库里的 `data/logs` 更像历史测试产物，不要把它当成唯一正式日志源。

## 8. 常见故障和处理办法

### 8.1 API 正常，Worker 不工作

现象：

- Web 能打开
- `GET /health` 正常
- 任务长时间不推进

处理：

1. 先确认 `zhihu-worker` 进程是否存活
2. 手动调用 `POST /worker/tick`
3. 查看 worker 日志里是否有 `[worker] tick failed`
4. 再看数据库里是否有卡住的任务状态

推荐 SQL：

```sql
SELECT id, status, current_stage, scheduled_at, updated_at, failure_reason
FROM publish_jobs
ORDER BY id DESC
LIMIT 50;
```

### 8.2 任务卡在 `manual_login_required`

这通常不是程序崩了，而是账号登录态失效、风控、账号不匹配或需要人工过验证。

处理：

1. 通过管理台发起人工登录
2. 在浏览器里完成知乎登录 / 验证
3. 再执行恢复确认

如果人工登录窗口已经关掉，但锁文件还在，可先确认没有遗留浏览器进程，再删：

```powershell
Remove-Item H:\claw\data\locks\manual-login-account-<accountId>.lock -Force
```

### 8.3 `network_or_page_error`，但机器网络明明是通的

这类问题经常不是“公网断了”，而是账号自己的浏览器 Profile 损坏、缓存异常、账号身份错乱，或者该 Profile 下的知乎会话已经变脏。

优先处理：

1. 关闭该账号相关的 Edge 窗口
2. 重新做人工登录恢复
3. 仍失败就备份并重建 `data/profiles/account-N/msedge`

重置示例：

```powershell
Rename-Item H:\claw\data\profiles\account-1\msedge msedge.bak-20260327
```

然后重新登录一次。

### 8.4 飞书没消息

先检查：

1. `FEISHU_BOT_WEBHOOK_URL`
2. `FEISHU_BOT_SECRET`
3. `POST /notifications/feishu/test`
4. API 日志

注意：

- 飞书失败不应阻塞主业务
- 飞书只是通知链路，不是任务主链路

### 8.5 任务成功了，但 `publish_attempts` 里还有旧的 `running`

这是已知问题，当前不会直接阻塞主链路，但会影响排障时的数据整洁度。

运维手工清理示例：

```sql
UPDATE publish_attempts
SET status = 'failed',
    failure_type = 'manual_cleanup',
    failure_reason = 'Ops cleaned stale running attempt'
WHERE publish_job_id = <jobId>
  AND status = 'running';
```

## 9. 运维常用 SQL

最近任务：

```sql
SELECT id, account_id, status, current_stage, scheduled_at, updated_at, failure_reason, final_url
FROM publish_jobs
ORDER BY id DESC
LIMIT 50;
```

最近发布尝试：

```sql
SELECT id, publish_job_id, attempt_no, status, failure_type, current_url, created_at
FROM publish_attempts
ORDER BY id DESC
LIMIT 50;
```

最近证据链：

```sql
SELECT id, publish_job_id, stage, tool_name, action, success, error_message, created_at
FROM tool_traces
ORDER BY id DESC
LIMIT 100;
```

账号状态：

```sql
SELECT id, name, zhihu_user_name, status, status_reason, profile_dir, last_login_check_at, last_publish_at
FROM accounts
ORDER BY id ASC;
```

今天的调度槽：

```sql
SELECT id, account_id, schedule_date, scheduled_at, status, publish_job_id
FROM daily_publish_schedule
WHERE schedule_date = CURRENT_DATE()
ORDER BY scheduled_at ASC;
```

## 10. 当前已知问题和运维可自行修改的小 bug

### 10.1 `.env.example` 少了 `FEISHU_BOT_SECRET`

现状：

- 代码支持 `FEISHU_BOT_SECRET`
- 示例文件没有这行

影响：

- 新环境照着 `.env.example` 配，容易漏掉飞书签名 secret

最小修改：

在 `.env.example` 里补一行：

```env
FEISHU_BOT_SECRET=
```

### 10.2 `.env.example` 里的 `TOPIC_KEYWORDS` 是乱码

现状：

- `.env.example` 的 `TOPIC_KEYWORDS` 内容已经乱码
- 真正可用的默认值可以参考 `packages/core/src/config/env.ts`

影响：

- 运维抄示例时容易把错误关键词带进生产

最小修改：

- 直接把 `.env.example` 里的 `TOPIC_KEYWORDS` 改成正常中文
- 或按业务自己填一组关键词，不要照抄乱码值

### 10.3 泛化的 `network_or_page_error` 会误伤题目有效性

现状：

- 当前代码在任务失败进入 `failed_terminal` 时，如果失败类型是 `network_or_page_error`，会把对应 `topic candidate` 标成 `invalid`

影响：

- 浏览器异常、知乎临时抽风、账号 Profile 损坏时，原本是好题目也可能被错误打成无效

代码位置：

- `packages/core/src/services/worker-runner.ts`

最小修改建议：

- 去掉“`network_or_page_error` 直接标记题目 invalid”这条逻辑
- 只让明确的 `topic_invalid` 去更新题目有效性

### 10.4 Profile 异常被统称为通用网络错误

现状：

- `page.goto()` 报错、超时、连接被关闭，最后大多会归一成 `network_or_page_error`

影响：

- 运维看到的是泛化错误，定位不到“账号 Profile 已坏”

代码位置：

- `packages/core/src/services/playwright-tool-runtime.ts`
- `packages/core/src/services/worker-runner.ts`
- `packages/core/src/services/session-service.ts`

最小修改建议：

- 单独加一个错误类型，例如 `browser_profile_error`
- 遇到只在单个账号 Profile 复现的 `goto` 异常时，直接提示“重置 Profile”

### 10.5 旧的 `publish_attempts` 可能残留 `running`

现状：

- 恢复、重试或异常中断后，早期 `publish_attempts` 可能没有完全收口

影响：

- 不影响最终发布结果
- 会影响排障和统计

代码位置：

- `packages/core/src/repositories/job-repository.ts`

最小修改建议：

- 在任务结束、重试、人工恢复确认后，批量把同 job 旧的 `running` 尝试收口为 `failed` 或 `aborted`

## 11. 当前不建议对外承诺的能力

这部分不是小 bug，而是当前阶段的真实边界，运维和业务都要知道：

- “按时自动发布”还不能按生产级 SLA 承诺
- 原因不是单点故障，而是完整链路很长，任务可能在到点时还停留在选题、写作、审核等前置阶段
- 如果要上线，建议先按“内部使用 / 人工盯盘 / 可回滚”方式运行，不要直接承诺严格准点发布

## 12. 生产建议

- API 必须放在内网或反向代理鉴权后面，不要裸露到公网
- 生产 `.env` 不要提交仓库
- `NEXT_PUBLIC_API_BASE_URL` 变更后一定重新构建前端
- 浏览器 Profile 目录要做备份策略，但不要频繁热拷贝正在使用的目录
- MySQL 建议每日备份
- `data/screenshots`、`output/playwright` 和数据库证据链建议保留一段时间，至少满足排障窗口

